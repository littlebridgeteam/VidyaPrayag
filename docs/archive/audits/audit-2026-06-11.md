# FINAL PRE-PRODUCTION STATIC-ANALYSIS AUDIT — 2026-06-11

**Repository:** vidyaprayag — multi-tenant school ERP+CRM
**Stack:** Kotlin Compose Multiplatform (composeApp) · shared KMP module · Ktor server (Exposed ORM) · Supabase Postgres
**Method:** 100% static analysis. No builds, no Gradle, no runtime. Every screen file, every SQL file, every ViewModel/Repository/API/Route in the data chains below was read before any finding was written (HARDLINE-1).
**Branch audited:** `backend-by-abuzar_v1.0.2` @ `b577215`
**Finding count:** 63 (HARDLINE-6 satisfied: ≥60)

---

## 1. SEVERITY LEGEND

| Severity | Meaning | Production gate |
|---|---|---|
| **CRITICAL** | Fabricated data shown as real, schema-vs-code divergence that 500s in prod, broken end-to-end business pipelines, unreachable mandatory flows | **MUST fix before any production traffic** |
| **HIGH** | Wrong-data/wrong-tenant risk, hardcoded UI state lying to users, dead account-recovery, unreachable shipped features, error-masking logic | Fix before public launch |
| **MEDIUM** | Dead interactive elements (buttons/rows that do nothing), double-submit risks, free-text date entry, UX contradictions between surfaces | Fix in first stabilization sprint |
| **LOW** | Dead code, no-op expressions, brand inconsistencies, duplicated modifiers/helpers, misplaced modules | Fix opportunistically |

Every finding carries: severity · exact `file:line` chain · the six-question trace (HARDLINE-2: data real? → ViewModel → Repository/API → server route → Supabase table) · fix specification (HARDLINE-7).

---

## 2. SCHEMA MAP

Canonical DDL: `scripts/schema-all-in-one-2026-06-07.sql` (~38 tables) + `scripts/schema-patch-2026-06-07.sql` + `scripts/schema-patch-2026-06-08-part2.sql` (notifications, device_tokens, parent_child_links). Run order: `scripts/README-RUN-ORDER.md`. `AUTO_CREATE_TABLES` is OFF in production — Exposed will NOT create missing tables.

**Core tenancy spine:** `schools` ← `school_id` FK on: `app_users`, `students`, `teachers`, `class_assignments`, `announcements`, `notifications`, `message_threads`, `leave_requests`, `admission_enquiries`, `ptm_events`, `attendance_records`, `marks_records`, `fee_ledger`, `parent_child_links`, `syllabus_coverage`, `school_calendar_events`, `app_config`, …

**Server table objects:** `server/.../db/Tables.kt` — Exposed table definitions mirror the SQL with **one CRITICAL divergence**:

> ⚠️ `NonTeachingStaffTable` (`Tables.kt:883`) has **no corresponding `CREATE TABLE non_teaching_staff`** in any SQL file under `scripts/`. With AUTO_CREATE_TABLES off, every `/api/v1/school/staff` query 500s in production. → **F-53 (CRITICAL)** and **F-40**.

**Notification spine:** `notifications` table written exclusively via `server/.../feature/notifications/Notify.kt` (`Notify.toUsers/toUser`, batchInsert with `schoolId` — tenant-isolated). 13 trigger call sites (full inventory in §7.2).

**Messaging model:** `message_threads` — RA-51 two-row design: each two-party conversation = TWO thread rows (one per owner) sharing `conversation_id`. Engine: `server/.../feature/school/MessagingCore.kt` (`sendInConversation`, `conversationMessagesFor` — ownership-scoped reads).

---

## 3. API SURFACE MAP

All routes JWT-authenticated unless noted. Server guards (verified solid, §7.4):
- `requireSchoolContext()` / `requireSchoolAdmin()` — `server/.../core/SchoolAccess.kt` (RA-34 `is_active`, RA-39 role from DB not JWT; `SCHOOL_ADMIN_ROLES` excludes `school_staff`)
- `requireTeacherContext()` / `requireOwnedAssignment()` — `server/.../core/TeacherAccess.kt` (assignment-scoped; ⚠ name-fallback, F-58)
- 21 routing files use the centralized guards (grep-verified).

| Surface | Routes (client method → server route) |
|---|---|
| **Auth** | login/OTP/refresh per AuthRepositoryImpl |
| **Notifications (role-agnostic)** | `GET /api/v1/notifications` (`ParentApi.kt:59` → `NotificationsRouting.kt:83-86`), `GET …/summary` (:178), `PATCH …/{id}/read` (:220 / `ParentApi.kt:66`), `POST …/read-all` (:235 / `ParentApi.kt:73`), `POST …/device-token` (:252) |
| **Parent** | profile, children, academics, activity, fees, scholarships, leave (`GET/POST /parent/leaves`), link-child (`ParentLinkRouting`), messages: `GET /threads`, `GET /recipients`, `GET /threads/{id}/messages`, `POST /threads/{id}/read`, `POST send` (:311) |
| **Teacher** | classes, attendance, marks, homework, syllabus (`TeacherRoutingTasks`), leave (`TeacherLeaveRouting`), messages: `GET /threads` (:165), `GET messages` (:181), `POST read` (:230), `POST send` (:248), `POST /class` broadcast (:283) |
| **School admin** | `GET/PUT /school/profile`, `GET/POST/DELETE /school/students`, `GET /school/teachers`, `GET /school/staff` ⚠F-40, `GET /school/{attendance/summary, marks/summary, fees/ledger}`, `GET /syllabus-coverage`, `GET/POST /api/v1/announcements` (RA-23/49), `GET /school/leaves?type=` + `POST /school/leaves/{id}/status`, `GET /school/link-requests?status=pending` + `POST …/{id}/approve|reject` (RA-48), `GET /admissions/enquiries/summary` + `PATCH …/enquiries/{id}/status`, `GET /school/results?test=&class=&subject=`, **`POST /api/v1/school/results`** (publish, `ResultsRouting.kt:321`, idempotent upsert, RA-S09 notify :388-398 — ⚠ not exposed by ResultsPublishScreenV2, F-35), `GET/POST /school/ptm`, `GET /school/attendance/daily` + `PATCH /school/attendance/{id}/status`, `GET /school/analytics/{overview,class,teachers}`, `GET /student-cohort`, messages: `GET /school/messages/threads` (:133), `GET messages` (:151), `POST read` (:204), `POST send` (:224) |
| **Discovery/public** | `GET /api/v1/school/calendar?date=&view_type=` — ⚠ **NO enquiry-submit endpoint is called by the Discovery UI** (F-21/22/54) |

---

## 4. NAVGRAPH MAP

State-driven navigation, **no NavHost**: `composeApp/.../ui/v2/navigation/NavGraphV2.kt` + portal-local overlay enums + `BackHandler`.

```
NavGraphV2
├── UnauthRoute.Splash → CommonLanding
├── UnauthRoute.CommonLanding → {ParentAuth, AdminAuth, TeacherFirstLogin, LegalInfo}
├── UnauthRoute.ParentAuth → (OTP) → ParentLinkChild → ParentPortal
├── UnauthRoute.AdminAuth → SchoolPortal
├── UnauthRoute.TeacherFirstLogin → TeacherPortal
├── UnauthRoute.Discovery        ⚠ UNREACHABLE from any unauth screen (F-19)
└── UnauthRoute.SchoolOnboarding ⚠ UNREACHABLE — no caller navigates here (F-20)

SchoolPortalV2 — 5 tabs (Home, People, Records, Comms, Settings) + 18 overlay enums
  (Messages, LeaveRequests, LinkRequests, AdmissionsCrm, ResultsPublish, SchedulePtm,
   DailyAttendance, ClassPerformance, TeacherPerformance, AnalyticsDashboard,
   EditSchoolProfile, StudentRoster, Student/Teacher/Staff profiles, PEWS, Notifications, …)
  commsBadge: REAL (SchoolPortalV2.kt:79)

TeacherPortalV2 — tabs + overlays; RA-40 update-selector wired (:135-163, VERIFIED FIXED)
  badge: ⚠ HARDCODED = 1 (TeacherPortalV2.kt:93, F-41)

ParentPortalV2 — tabs (Home, Academics, Activity, Messages, Profile) + overlays
  (Fees, Leave, AttendanceCalendar, Notifications, Discovery overlay, …)
  ⚠ Scholarships overlay: enum exists, NO entry point sets it (F-45)
  ⚠ Calendar overlay: enum exists, NO entry point sets it (F-46)
```

Back navigation: layered (compose→conversation→list→exit verified in MessagesScreenV2; VBackHeader pattern consistent across overlays). Loading/Error/Empty: VStateHost (Shared.kt) — 4 phases + skeleton crossfade; honest placeholders via VComingSoon (LAW 6).

---

## 5. KOIN SCOPE MAP

`shared/.../di/Koin.kt`:

| Kind | Scope | Notes |
|---|---|---|
| ALL ViewModels | `factory {}` | fresh per injection — correct with SessionScope |
| API clients | `single {}` | stateless, OK |
| Repositories | `single {}` | stateless, OK |
| `SelectedChildHolder` | `single {}` | cleared on logout (RA-S05 ✅ verified) |
| `PreferenceRepository` | `single {}` | token storage |

**Session lifecycle:** `App.kt:220-240` — `SessionScope` = per-login `ViewModelStoreOwner` keyed by JWT token. New login ⇒ new store ⇒ no ViewModel state bleed across users. ✅ VERIFIED — no findings; this is the correct architecture for factory-scoped VMs.

---

## 6. AUDIT BY ROLE → SCREEN → ELEMENT

Per-element block format (HARDLINE-2): **Q1** real or hardcoded data? **Q2** ViewModel field. **Q3** Repository/API method. **Q4** server route. **Q5** Supabase table/columns. **Q6** gap ⇒ finding.

### 6.1 ROLE: UNAUTHENTICATED

#### Screen: SplashScreenV2 — `composeApp/.../screens/auth/SplashScreenV2.kt`
- **Wordmark** (line 160): renders **"VidyaSetu"** while the product/repo is **VidyaPrayag**. Q1 hardcoded string. Q2–Q5 N/A (branding). Q6 gap → **F-01 (LOW)**. Same string family appears at CommonLandingScreenV2.kt:226 vs :688 (inconsistent) and SchoolSettingsScreenV2 mailto subject `VidyaSetu%20Support`. **Fix:** single `BrandConstants.APP_NAME`, referenced everywhere.
- **Auto-route on token**: reads `PreferenceRepository` token → routes portal. ✅ wired.

#### Screen: CommonLandingScreenV2 — `…/auth/CommonLandingScreenV2.kt`
- **Portal cards (Parent/School/Teacher)**: navigate via NavGraphV2 callbacks. ✅ wired.
- **"View All Portals" button** (lines 692-697): Q1 UI affordance. Q6 **onClick does nothing / no destination exists** → **F-02 (MEDIUM)** dead button. **Fix:** remove the button or wire to a portal-list sheet.
- **Brand string** (226 vs 688): two different brand spellings on one screen → folded into **F-01**.

#### Screen: AuthScaffoldV2 — `…/auth/AuthScaffoldV2.kt`
- **"End-to-end encrypted" claim** (line 183): Q1 hardcoded marketing claim. Q5 transport is HTTPS+JWT; there is **no E2E encryption anywhere in the codebase** (no client-side crypto, server reads plaintext bodies). Q6 false security claim → **F-03 (HIGH)** — legal/compliance exposure. **Fix:** change copy to "Encrypted in transit" or remove.

#### Screen: ParentAuthScreenV2 — `…/auth/ParentAuthScreenV2.kt`
- **Phone + OTP flow**: Q2 AuthViewModel state → Q3 AuthRepositoryImpl → Q4 auth/OTP routes → Q5 `app_users`. ✅ wired.
- **OTP resend**: Q6 **no resend button and no countdown timer** — a user whose OTP never arrives is stuck and must back out and restart → **F-04 (MEDIUM)**. **Fix:** add resend with 30s cooldown calling the same OTP-request method.

#### Screen: AdminAuthScreenV2 — `…/auth/AdminAuthScreenV2.kt`
- **Email/password login**: ✅ wired through AuthViewModel → `app_users`.
- **"Forgot Password?" link** (~line 117): Q1 affordance. Q3/Q4 **no reset method exists in any repository, no server route** → tap does nothing → **F-05 (HIGH)** — dead account-recovery on the admin surface. **Fix:** either implement `POST /api/v1/auth/password-reset` (email token flow) or remove the link and document manual recovery via support.

#### Screen: TeacherFirstLoginScreenV2 — `…/auth/TeacherFirstLoginScreenV2.kt`
- **Greeting name** (line 55): Q1 default literal **"Mr. Vikram"** shown before/if profile load fails → fabricated identity → **F-06 (MEDIUM)**. **Fix:** default to empty/"Teacher", never a fake person.
- **Help link** (lines 134-140): Q6 onClick empty → **F-07 (MEDIUM)** dead help affordance. **Fix:** mailto SUPPORT_EMAIL (constant exists at LegalInfoScreenV2.kt:48).
- **Credential set + first login**: ✅ wired (RA-22 family).

#### Screen: ParentLinkChildScreenV2 — `…/auth/ParentLinkChildScreenV2.kt` + `shared/.../parent/presentation/LinkChildViewModel.kt`
- **School search**: Q2 `LinkChildViewModel.searchSchools()` → Q4 school search route → Q5 `schools`. ✅ wired.
- **Match selection (step 2)**: Q1 real matches rendered (lines 204-229) **but the match cards have NO onClick**. Q2 the VM exposes `selectSchool(match)` (LinkChildViewModel.kt:77) — **never called by any UI**. The VM auto-selects `schools.firstOrNull()` (LinkChildViewModel.kt:101). Q6 **a parent can never choose among multiple matching schools; the first match always wins** — wrong-school link risk in cities with similarly-named schools → **F-08 (HIGH)** + dead VM API → **F-09 (LOW)**. **Fix:** add `onClick = { vm.selectSchool(match) }` to each match card and a selected-state visual; keep auto-select only when exactly one match.
- **Link submit**: Q3 link-child API → Q4 `ParentLinkRouting` → Q5 `parent_child_links` (pending vs approved handled, RA-48). ✅ wired.

#### Screen: LegalInfoScreenV2 — `…/auth/LegalInfoScreenV2.kt`
- Static legal copy + SUPPORT_EMAIL constant (line 48). ✅ no findings.

#### Screen: SchoolOnboardingScreenV2 — `…/auth/SchoolOnboardingScreenV2.kt` — ⚠ FABRICATION CLUSTER
This screen presents an onboarding wizard whose data is substantially fake:
- **Confirmation email step** (lines 188-192): Q1 **fabricated email address rendered as "sent"** — no Q3 repository call, no Q4 route, nothing in Q5. → **F-10 (CRITICAL)**. **Fix:** wire to a real onboarding-email endpoint or remove the step.
- **Seed teacher list** (lines 146-152): Q1 hardcoded teacher names presented as imported staff. Q2 none. → **F-11 (CRITICAL)**. **Fix:** drive from `GET /school/teachers` post-creation, or show honest empty state.
- **CSV import stats** (lines 716-725): Q1 fabricated "N rows imported / M skipped" numbers — **no file is ever parsed or uploaded** (no Q3/Q4). → **F-12 (CRITICAL)**. **Fix:** implement multipart upload + server CSV parse, or replace with VComingSoon.
- **Completion stats** (lines 793-816): Q1 hardcoded counts on the success screen. → **F-13 (CRITICAL)**. **Fix:** fetch real counts or remove numbers.
- **Dead buttons** (lines 657, 713, 824): onClick no-ops on primary actions. → **F-14 (HIGH)**. **Fix:** wire or remove.
- **Reachability:** `UnauthRoute.SchoolOnboarding` is never navigated to (NavGraphV2) → **F-20 (HIGH)** — the whole wizard is dead code in the shipping nav graph; this *contains* the fabrications but does not excuse them since the route enum is live and one nav change exposes it. **Fix:** either finish + expose the wizard or delete route + screen.

#### Route: UnauthRoute.Discovery
- Enum exists in NavGraphV2; **no unauthenticated screen navigates to it** (Discovery is reachable only as a ParentPortal overlay post-login). → **F-19 (MEDIUM)** unreachable route. **Fix:** add a "Find schools" entry on CommonLanding, or remove the unauth route.

### 6.2 ROLE: PARENT (incl. Discovery overlay)

#### Screen: ParentPortalV2 — `…/screens/parent/ParentPortalV2.kt`
- **Tabs Home/Academics/Activity/Messages/Profile**: ✅ wired.
- **Scholarships overlay**: enum value exists; **no button/row anywhere sets it** → unreachable shipped screen → **F-45 (HIGH)**. Note ParentScholarshipsScreenV2 itself is wired (see below) — the *only* defect is reachability. **Fix:** add entry row in ParentProfile or ParentFees.
- **Calendar overlay**: same pattern — AcademicCalendarScreenV2 is fully wired but **unreachable from ParentPortal** → **F-46 (HIGH)**. **Fix:** entry card on ParentHome or Academics tab.

#### Screen: ParentHomeScreenV2 — ✅ CLEAN. Real child summary via SelectedChildHolder → parent APIs → `students`/`attendance_records`/`marks_records`.

#### Screen: ParentAcademicsScreenV2 — ✅ CLEAN. Wired marks/homework/syllabus chains.

#### Screen: ParentActivityScreenV2 — ✅ CLEAN.

#### Screen: ParentMessagesScreenV2 — ✅ CLEAN. Q2 parent MessagesViewModel → Q3 `ParentApi` → Q4 `GET /threads`, `GET /recipients`, `GET /threads/{id}/messages`, `POST /threads/{id}/read`, `POST send` (:311) → Q5 `message_threads` (RA-51 two-row). composeNew recipient picker wired.

#### Screen: ParentFeesScreenV2 — `…/parent/ParentFeesScreenV2.kt`
- **Fee ledger**: ✅ wired → `fee_ledger`.
- **"Message the school →" row** (lines 166-170): Q6 **onClick does nothing** — it should deep-link to Messages composeNew with the school pre-selected → **F-15 (MEDIUM)**. **Fix:** invoke the portal's open-Messages callback with `composeNew=true`.

#### Screen: ParentLeaveScreenV2 — `…/parent/ParentLeaveScreenV2.kt`
- **Leave list + submit**: ✅ wired → `POST /parent/leaves` → `leave_requests` (Notify trigger at ParentLeaveRouting:189 confirmed).
- **Date fields** (lines 122-136): Q1 **free-text "YYYY-MM-DD" strings** — no picker, no client validation; malformed dates rely on server rejection → **F-16 (MEDIUM)**, part of the recurring free-text-date pattern (with F-32, F-37 — see §7.8). **Fix:** shared VDatePicker component.

#### Screen: ParentProfileScreenV2 — `…/parent/ParentProfileScreenV2.kt`
- **Profile data + logout**: ✅ wired (RA-21 confirm).
- **Two settings rows** (lines 164-165): Q6 onClick=null but rendered as tappable → **F-17 (MEDIUM)** dead rows. **Fix:** wire or convert to VComingSoon style without chevron.

#### Screen: ParentScholarshipsScreenV2 — `…/parent/ParentScholarshipsScreenV2.kt`
- **Scholarship list**: ✅ wired chain.
- **Error state** (line 98): `onRetry = null` — error renders with **no retry affordance** while VStateHost supports it → **F-18 (MEDIUM)**. **Fix:** pass `onRetry = { vm.load() }`.
- Also unreachable (F-45 above).

#### Screen: ParentAttendanceCalendarScreenV2 — ✅ CLEAN.

#### Screen: NotificationsScreenV2 — `…/screens/notifications/NotificationsScreenV2.kt`
- **Feed**: Q2 NotificationsViewModel → Q3 `ParentApi.kt:59 GET api/v1/notifications` → Q4 `NotificationsRouting.kt:83` (verified **role-agnostic**: route reads role from `app_users` at :115 and serves admins/teachers too — the SchoolHome comment is accurate; the VM's *KDoc* at NotificationsViewModel.kt:34 still says `/parent/notifications`, stale doc only) → Q5 `notifications`. ✅ wired. Stale KDoc → folded into **F-60 (LOW)**.
- **Preferences footer** (line 284): Q6 dead row, no prefs screen/route exists → **F-44 (MEDIUM)**. **Fix:** remove or VComingSoon.

#### Screen: AiReportCardPreview — `…/parent/AiReportCardPreview.kt` — ✅ CLEAN (RA-S11 honest label-free schematic, no fabricated numbers).

#### Screen: DiscoveryScreenV2 — `…/screens/discovery/DiscoveryScreenV2.kt` — ⚠ BROKEN BUSINESS PIPELINE
- **School search/list**: ✅ wired.
- **Enquiry submit — sheet** (lines 529-538) and **inline form** (lines 647-656): Q1 user enters real data; submit handler **shows a success state without calling anything** — Q3 no repository method, Q4 no route invoked, Q5 nothing written to `admission_enquiries`. Meanwhile **AdmissionsCrmScreenV2 (admin) reads `GET /admissions/enquiries/summary` from that very table** — the CRM funnel's parent-side intake is fake. Parent believes an enquiry was sent; school never receives it. → **F-21 (CRITICAL)** + cross-cutting pipeline finding **F-54 (CRITICAL)**. **Fix:** add `POST /api/v1/admissions/enquiries` (public or parent-JWT), client API method, wire both forms; show error states.
- **Filter chips** (lines 193-196): Q6 dead — selecting changes nothing → **F-22 (MEDIUM)**. **Fix:** wire to query params or remove.
- **♡ / Compare buttons** (lines 403-404): Q6 dead → **F-23 (MEDIUM)**. **Fix:** wire compare (SchoolCompare exists per commit 1dd6666) and favorites, or hide.

#### Screen: AcademicCalendarScreenV2 — `…/discovery/AcademicCalendarScreenV2.kt`
- **Month grid + events**: ✅ wired — AcademicCalendarViewModel → `GET /api/v1/school/calendar?date=&view_type=` → `school_calendar_events`.
- **EventRow chevron** (line 225): renders ChevronRight with **no onClick** → dead affordance → **F-24 (LOW)**. **Fix:** remove chevron or add detail sheet.

#### Screen: SriPreview — `…/discovery/SriPreview.kt` — ⚠ UNSANITIZED FABRICATION
- Q1 **hardcoded fabricated metrics rendered with real-looking labels**: 6 signals (lines 41-48: "Academic outcomes" 92, "Teacher retention" 84, "Parent sentiment" 78, "Safety & infra" 88, "Co-curricular" 71, "Attendance norms" 86), "Above Lucknow median (7.4)" (line 59), "+0.3 YoY" chip (line 68). Q2–Q5 none — pure literals. Its siblings PewsPreview (RA-S10) and AiReportCardPreview (RA-S11) were scrubbed to label-free schematics; SriPreview was missed. → **F-25 (HIGH)**. **Fix:** apply the same RA-S10/S11 sanitization — abstract bars, no numbers, no city claims.

### 6.3 ROLE: TEACHER

#### Screen: TeacherPortalV2 — `…/teacher/TeacherPortalV2.kt`
- **RA-40 update-selector** (lines 135-163): explicitly re-verified per audit mandate — the class/subject selector is **wired** to real assignment data and propagates the selection to attendance/marks/homework screens. ✅ RA-40 FIXED, confirmed.
- **Badge** (line 93): Q1 **hardcoded `badge = 1`** on the nav item — lies "you have 1 unread" forever. Q2 should come from NotificationsViewModel `unreadCount` (the route is role-agnostic, NotificationsRouting.kt:115). Q6 gap → **F-41 (HIGH)**. **Fix:** inject NotificationsViewModel, bind summary unread count (same pattern as SchoolPortalV2.kt:79 which is real).

#### Screen: TeacherHomeScreenV2 — ✅ CLEAN. Real assignment-scoped data.

#### Screen: TeacherClassesScreenV2 — `…/teacher/TeacherClassesScreenV2.kt`
- **Class list**: ✅ wired → `class_assignments`.
- **Edit icon** (line 174): Q6 dead — no edit flow exists → **F-26 (MEDIUM)**. **Fix:** remove icon (class edits are admin-side) or wire.

#### Screen: TeacherAttendanceScreenV2 — ✅ CLEAN (RA-S18). Q4 `TeacherRoutingTasks` attendance write (Notify trigger :293) → `attendance_records`.

#### Screen: TeacherMarksScreenV2 — ✅ CLEAN (RA-S18). Notify trigger TeacherRoutingTasks:449 → `marks_records`.

#### Screen: TeacherHomeworkScreenV2 — `…/teacher/TeacherHomeworkScreenV2.kt`
- **Homework list + create flow**: ✅ wired (Notify trigger TeacherRoutingTasks:696).
- **"Assign new homework" entry** (line 55): Q6 **dead** — onClick no-op, duplicating the working FAB/flow elsewhere on the screen → **F-27 (MEDIUM)**. **Fix:** route it to the same create flow or remove.

#### Screen: TeacherSyllabusScreenV2 — ✅ CLEAN → `syllabus_coverage`.

#### Screen: TeacherLeaveScreenV2 — ✅ CLEAN (RA-44) → `TeacherLeaveRouting` (decision Notify :178) → `leave_requests`.

#### Screen: TeacherMessagesScreenV2 — ✅ CLEAN. Threads/messages/read/send + **class broadcast** `POST /class` (TeacherMessagesRouting:283, Notify :325).

#### Screen: TeacherProfileScreenV2 (teacher-side) — `…/teacher/TeacherProfileScreenV2.kt`
- **Profile data + logout**: ✅ wired.
- **3 settings rows** (lines 140-155): Q6 onClick=null, rendered tappable → **F-28 (MEDIUM)** dead rows. **Fix:** wire or strip affordance.

#### Screen: TeacherUpdateSelector — ✅ CLEAN (RA-40 component).

### 6.4 ROLE: SCHOOL ADMIN

The best-wired vertical in the app — 22 screens read, most genuinely end-to-end. Findings below are the deltas.

#### Screen: SchoolPortalV2 — ✅ CLEAN. 5 tabs, 18 overlays; commsBadge REAL (line 79).

#### Screen: SchoolHomeScreenV2 — ✅ CLEAN. SchoolDashboardViewModel (6 flows) + NotificationsViewModel real bell dot (RA-S06); onboarding hero real; MetricEntryCards → Analytics/PEWS (RA-24).

#### Screen: SchoolPeopleScreenV2 — `…/school/SchoolPeopleScreenV2.kt` (RA-S17)
- **3 sub-tabs** via 4 VMs (SchoolTeachersViewModel `/school/teachers`, StudentRosterViewModel `/school/students`, StaffViewModel `/school/staff` ⚠F-40, StudentAnalyticsViewModel `/student-cohort`). AddTeacherDialog (RA-22), AddStaffDialog, link-requests entry (RA-48), no row deletion (RA-S17 principle). ✅ mostly clean.
- **`onStudentSearch = { studentsViewModel.load() }`** (line 106): ignores its `String` param — search re-loads full roster; filtering happens client-side anyway → **F-29 (LOW)** misleading API. **Fix:** drop the param or pass it through to a `?q=` query.
- **Staff tab data**: Q4 `GET /school/staff` → Q5 `non_teaching_staff` — **table absent from SQL** → 500 in prod → **F-40 (CRITICAL**, the client-visible face of F-53**)**.

#### Screen: SchoolRecordsScreenV2 — ✅ CLEAN. Coverage (`/syllabus-coverage`), RA-52 Attendance/Marks/Fee lazy tabs (`/school/{attendance/summary,marks/summary,fees/ledger}`); Documents = honest VComingSoon.

#### Screen: SchoolCommsScreenV2 — `…/school/SchoolCommsScreenV2.kt`
- **Announcements**: ✅ wired GET/POST `/api/v1/announcements`, RA-23 compose, RA-49 audience targeting (ALL_SCHOOL/CLASS/SUBJECT/STUDENT), shake-on-error, VPullRefresh. Notify trigger AnnouncementRouting:245.
- **Date input** (lines 371-378): free-text "e.g. 2026-06-20" → **F-32 (MEDIUM)**, recurring pattern (§7.8).
- **Notifications tab**: honest VComingSoon "Delivery log" ✅.

#### Screen: SchoolSettingsScreenV2 — `…/school/SchoolSettingsScreenV2.kt`
- **Institutional profile**: ✅ wired (`GET /api/v1/user/profile`); RA-47 Edit row; RA-21 logout; Help mailto.
- **5 "Coming Soon" rows** (Academic year, Classes & subjects, Fee structure, Notifications, Data export; rows 172-179): `onClick = null` **but the shared row composable unconditionally renders ChevronRight (line 206)** — five rows that look navigable and do nothing → **F-30 (MEDIUM)**. **Fix:** chevron only when onClick != null; add "Soon" VBadge.

#### Screen: MessagesScreenV2 (admin) — ✅ CLEAN. MessagesViewModel → `GET /school/messages/threads` (:133), `POST send` (:224), `POST read` (:204); RA-S07 composeNew with teacher picker; layered back; send button proper loading.

#### Screen: LeaveRequestsScreenV2 — `…/school/LeaveRequestsScreenV2.kt`
- **List + decide**: ✅ wired `GET /school/leaves?type=`, `POST /school/leaves/{id}/status` (Notify LeaveRequestsRouting:306).
- **Approve/Reject VButtons** (lines 195-216): **no per-row loading/disabled state** (LinkRequestsScreenV2 solved this with `actingIds`) — rapid double-tap fires duplicate status posts → **F-31 (MEDIUM)**. **Fix:** copy the `actingIds` pattern.

#### Screen: LinkRequestsScreenV2 — ✅ CLEAN (RA-48; per-row `acting` state present — the reference implementation for F-31/F-33).

#### Screen: AdmissionsCrmScreenV2 — `…/school/AdmissionsCrmScreenV2.kt`
- **KPIs + funnel**: ✅ wired `GET /admissions/enquiries/summary`, `PATCH …/{id}/status` → `admission_enquiries`. **But intake is broken** — see F-21/F-54: the parent-side enquiry form never writes to this table.
- **Funnel buttons** (Follow-up/Convert/Reject): no per-action loading → **F-33 (MEDIUM)** (same class as F-31). **Fix:** `actingIds` pattern.

#### Screen: ResultsPublishScreenV2 — `…/school/ResultsPublishScreenV2.kt`
- **Browse results**: ✅ wired `GET /school/results?test=&class=&subject=`; 3 VTag filter rows.
- **Publish action**: Q1 the screen is *named* Publish; Q4 the server exposes **`POST /api/v1/school/results`** (ResultsRouting.kt:321 — idempotent upsert, fires "Results published" Notify :388-398 RA-S09); Q3/Q2 **no client API method or VM action calls it; the screen is read-only**. The only writer of marks-with-publish-notify is the teacher flow — the admin "Publish" surface is a misnomer with an orphaned server endpoint → **F-35 (HIGH)**. **Fix:** either add a publish/import action wired to `POST /school/results`, or rename the screen "Results" and document the endpoint as teacher/import-only.

#### Screen: SchedulePtmScreenV2 — ✅ wired `GET/POST /school/ptm` → `ptm_events`; inline composer with loading. **Date free-text "YYYY-MM-DD" (line 124)** → **F-37 (MEDIUM)** (pattern, §7.8).

#### Screen: DailyAttendanceScreenV2 — ✅ CLEAN. `GET /school/attendance/daily`, `PATCH /school/attendance/{id}/status`; Students/Faculty toggle; P/A/L pills.

#### Screens: ClassPerformanceScreenV2 / TeacherPerformanceScreenV2 / AnalyticsDashboardScreenV2 — ✅ ALL CLEAN. Wired to `GET /school/analytics/{class,teachers,overview}`; pure-Canvas chart in AnalyticsDashboard.

#### Screen: EditSchoolProfileScreenV2 — `…/school/EditSchoolProfileScreenV2.kt` (RA-47)
- **14 fields, GET/PUT `/school/profile`** (server resolves school_id from JWT ✅ tenant-safe). ✅ wired.
- **Inset modifiers duplicated**: `statusBarsPadding/imePadding/navigationBarsPadding` on outer Column (lines 58-60) AND inner Column (111-113) → double padding → **F-36 (LOW)**. **Fix:** remove inner set.

#### Screen: StudentRosterScreenV2 — `…/school/StudentRosterScreenV2.kt` (RA-45)
- **CRUD**: ✅ wired GET/POST/DELETE `/school/students`; add-dialog auto-close; removal confirm.
- **Per-row "Remove" buttons** (lines 177-184): **contradicts the RA-S17 deletion-in-profile-only principle** applied in SchoolPeopleScreenV2 — two student-list surfaces with opposite destructive-action UX → **F-34 (MEDIUM)**. **Fix:** drop row-level remove; deletion lives in StudentProfileScreenV2 (which already has it behind VConfirmDialog).

#### Screens: StudentProfileScreenV2 / TeacherProfileScreenV2 (admin-side) — ✅ CLEAN. RA-45 load(id); RA-S17 remove-in-profile behind VConfirmDialog; `LaunchedEffect(state.removed) → onRemoved()`.

#### Screen: StaffProfileScreenV2 — `…/school/StaffProfileScreenV2.kt`
- Resolves member from StaffViewModel's roster list (no dedicated fetch).
- **Error-masking auto-close** (lines 60-62): `LaunchedEffect(state.staff) { if (state.staff.none { it.id == staffId } && !state.isLoading) onRemoved() }` — **does not check `state.error`**. A roster load FAILURE (staff=[], isLoading=false, error!=null) instantly fires `onRemoved()`, silently closing the profile and hiding the error from the admin → **F-38 (HIGH)**. **Fix:** `if (state.error == null && !state.isLoading && state.staff.none { … }) onRemoved()`; render error state inside the profile otherwise. (Also downstream of F-40/F-53: with the table missing, this screen *always* error-closes in prod.)

#### Screen: PewsPreview — ✅ CLEAN (RA-S10 label-free schematic).

---

## 7. CROSS-CUTTING FINDINGS

### 7.1 Communication matrix (every sender→recipient pair, statically verified)

| # | Sender → Recipient | Mechanism | Client surface | Server route | Table | Status |
|---|---|---|---|---|---|---|
| C1 | Admin → Teacher (1:1) | RA-S07 composeNew | MessagesScreenV2 (admin) | `POST /school/messages` (:224) | message_threads | ✅ |
| C2 | Admin ↔ Parent (threads) | reply in thread | MessagesScreenV2 | `POST send` via MessagingCore | message_threads | ✅ |
| C3 | Parent → School/Teacher | composeNew + recipients picker | ParentMessagesScreenV2 | `GET /recipients`, `POST send` (:311) | message_threads | ✅ |
| C4 | Teacher → class parents (broadcast) | class broadcast | TeacherMessagesScreenV2 | `POST /class` (TeacherMessagesRouting:283) | message_threads + Notify :325 | ✅ |
| C5 | Teacher ↔ Parent (1:1 replies) | thread reply | TeacherMessagesScreenV2 | `POST send` (:248) | message_threads | ✅ |
| C6 | Admin → audience (announcements) | RA-23/49 targeting | SchoolCommsScreenV2 | `POST /api/v1/announcements` | announcements + Notify (AnnouncementRouting:245) | ✅ |
| C7 | Parent (prospect) → School CRM | enquiry form | DiscoveryScreenV2 :529-538/:647-656 | **NONE — never called** | admission_enquiries (read-only side exists) | ❌ **F-21/F-54 CRITICAL** |
| C8 | Parent → School (from Fees) | "Message the school →" | ParentFeesScreenV2:166-170 | dead onClick | — | ❌ **F-15** |

**F-54 (CRITICAL) — Broken admissions pipeline.** The only revenue-facing CRM funnel (AdmissionsCrmScreenV2 ← `admission_enquiries`) has **no functioning intake**: the parent enquiry forms fake success client-side. Six-question trace: Q1 real user input → Q2 no VM submit → Q3 no API method → Q4 no route → Q5 table only ever read. **Fix:** implement `POST /api/v1/admissions/enquiries` + ApiClient method + wire both Discovery forms with loading/error/success states.

### 7.2 Notification triggers — full inventory (13 call sites, all via Notify.kt spine)

| Trigger | Call site | Category |
|---|---|---|
| Announcement published | AnnouncementRouting:245 | announcement |
| Parent leave submitted | ParentLeaveRouting:189 | leave |
| Link request new/approved/rejected | ParentLinkRouting:302/476/527 | link_request |
| Parent leave decided | LeaveRequestsRouting:306 | leave |
| New message | MessagingCore:336 (`notifyMessageRecipient`, RA-S08) | message |
| Results published | ResultsRouting:394 (RA-S09) | marks |
| Teacher leave decided | TeacherLeaveRouting:178 | leave |
| Teacher class broadcast | TeacherMessagesRouting:325 | message |
| Attendance / Marks / Homework posted | TeacherRoutingTasks:293/449/696 | attendance/marks/homework |

All 13 write through `Notify.toUsers/toUser` with `schoolId` — tenant-isolated ✅. Findings in the spine:
- **F-50 (LOW)** — `Notify.kt:41`: `.distinct().filter { true }` — the `.filter { true }` is a no-op left over from an edit. **Fix:** delete it.
- **F-51 (MEDIUM)** — `MessagingCore.kt:343`: notification `deepLink` is **hardcoded `"parent/messages"`** and the fallback title (line 339) is **"your child's school"** *regardless of recipient role* — a teacher or admin receiving a message gets a parent-portal deep link and parent-flavored copy. **Fix:** branch deepLink/title on recipient role (already known at the call site).

### 7.3 Schema-vs-code divergence (HARDLINE-3)

- **F-53 (CRITICAL)** — `server/.../db/Tables.kt:883` defines `NonTeachingStaffTable`; **no `CREATE TABLE non_teaching_staff` exists** in `schema-all-in-one-2026-06-07.sql` nor either patch file. AUTO_CREATE_TABLES is off in prod ⇒ `GET /school/staff` (used by SchoolPeopleScreenV2 Staff tab + AddStaffDialog + StaffProfileScreenV2) throws at first query. Client-side blast radius: F-40, and F-38 makes the failure *silent* (auto-close). **Fix:** write `scripts/schema-patch-2026-06-11.sql` with the CREATE TABLE matching Tables.kt:883 column-for-column (incl. `school_id` FK + index), add to README-RUN-ORDER.md.
- All other ~38 tables cross-referenced Tables.kt ↔ SQL: ✅ no further divergence found.

### 7.4 Multi-tenancy / authorization

- ✅ `SchoolAccess.kt`: `requireSchoolContext` (401/403/404, RA-34 `is_active`, role read from **DB not JWT**), `requireSchoolAdmin` layered; `SCHOOL_ADMIN_ROLES` excludes `school_staff` (RA-39). SOLID.
- ✅ 21 routing files use the centralized guards (grep-verified); per-route `WHERE school_id` derived from guard context.
- ✅ `Notify` writes carry schoolId; `MessagingCore.conversationMessagesFor` is ownership-scoped.
- **F-58 (MEDIUM, security)** — `TeacherAccess.kt:126-128, 161`: `requireOwnedAssignment`'s fallback matches assignment ownership by `teacherName?.equals(ctx.fullName, ignoreCase = true)`. **Two same-named teachers in one school can cross-access each other's class assignments** (attendance/marks/homework writes). **Fix:** backfill `teacher_id` on `class_assignments`, remove the name fallback (or restrict it to rows where teacher_id IS NULL and log a warning).

### 7.5 ViewModel scoping & session lifecycle

✅ No findings. All VMs `factory{}` (Koin.kt); `App.kt:220-240` SessionScope per-login ViewModelStoreOwner keyed by JWT — prevents cross-user state bleed; `SelectedChildHolder` cleared on logout (RA-S05).

### 7.6 Hardcoded UI state

- **F-41 (HIGH)** — TeacherPortalV2.kt:93 `badge = 1` (detailed §6.3).
- **F-25 (HIGH)** — SriPreview fabricated metrics (§6.2).
- **F-10..F-13 (CRITICAL)** — SchoolOnboarding fabrication cluster (§6.1).
- **F-06 (MEDIUM)** — "Mr. Vikram" default (§6.1).

### 7.7 Dead-code / dead-element inventory

| ID | Item | Location |
|---|---|---|
| F-02 | "View All Portals" button | CommonLandingScreenV2:692-697 |
| F-05 | "Forgot Password?" | AdminAuthScreenV2:~117 |
| F-07 | Help link | TeacherFirstLoginScreenV2:134-140 |
| F-09 | `LinkChildViewModel.selectSchool` never called | LinkChildViewModel.kt:77 |
| F-14 | 3 dead onboarding buttons | SchoolOnboardingScreenV2:657/713/824 |
| F-15 | "Message the school →" | ParentFeesScreenV2:166-170 |
| F-17 | 2 dead profile rows | ParentProfileScreenV2:164-165 |
| F-19 | UnauthRoute.Discovery unreachable | NavGraphV2 |
| F-20 | UnauthRoute.SchoolOnboarding unreachable | NavGraphV2 |
| F-22 | Dead filter chips | DiscoveryScreenV2:193-196 |
| F-23 | Dead ♡/Compare | DiscoveryScreenV2:403-404 |
| F-24 | EventRow chevron no onClick | AcademicCalendarScreenV2:225 |
| F-26 | Dead Edit icon | TeacherClassesScreenV2:174 |
| F-27 | Dead "Assign new homework" | TeacherHomeworkScreenV2:55 |
| F-28 | 3 dead teacher-profile rows | TeacherProfileScreenV2 (teacher):140-155 |
| F-30 | 5 chevroned Coming-Soon rows | SchoolSettingsScreenV2:172-179 (chevron :206) |
| F-44 | Dead notification-prefs footer | NotificationsScreenV2:284 |
| F-45 | Scholarships overlay unreachable | ParentPortalV2 |
| F-46 | Calendar overlay unreachable | ParentPortalV2 |
| F-50 | `.filter { true }` no-op | Notify.kt:41 |

### 7.8 Recurring pattern findings

- **F-55 (MEDIUM, pattern)** — **Free-text date entry, no picker**, 3 surfaces: ParentLeaveScreenV2:122-136 (F-16), SchoolCommsScreenV2:371-378 (F-32), SchedulePtmScreenV2:124 (F-37). **Fix:** one shared `VDatePicker` in the V* design system, swap all three.
- **F-56 (MEDIUM, pattern)** — **Missing per-row action loading/disable** (double-submit): LeaveRequestsScreenV2:195-216 (F-31), AdmissionsCrmScreenV2 funnel buttons (F-33). Reference implementation already in-repo: LinkRequestsScreenV2 `actingIds`. **Fix:** extract `actingIds` into a small shared helper, apply to both.
- **F-57 (MEDIUM, pattern)** — **Destructive-action UX split**: StudentRosterScreenV2 row-level Remove (F-34) vs RA-S17 profile-only deletion in SchoolPeopleScreenV2 / profiles. **Fix:** standardize on profile-only.
- **F-59 (LOW)** — Module placement: Discovery's `AcademicCalendarScreenV2`/`SriPreview` live under `screens/discovery/` while consumed from the parent portal overlay. **Fix:** move or document the ownership.
- **F-61 (LOW)** — `AcademicCalendarScreenV2` carries private local date helpers that duplicate logic available in shared utils. **Fix:** consolidate into shared date utilities.
- **F-60 (LOW)** — Stale documentation: NotificationsViewModel.kt:34 KDoc cites `GET /api/v1/parent/notifications`; the real client call is `api/v1/notifications` (ParentApi.kt:59) and the route is role-agnostic (NotificationsRouting.kt:6 comment + :115 role read). Also NotificationsViewModel lives under `feature/parent/` though it serves all three roles. **Fix:** correct KDoc; optionally move to `feature/notifications/`.
- **F-62 (MEDIUM)** — **OTP UX**: no resend/timer (F-04) combined with no error-recovery copy on ParentAuth — first-login funnel fragility. **Fix:** resend + cooldown + "didn't get a code?" help.
- **F-63 (LOW)** — `ResultsRouting.kt` publish endpoint (POST :321) has no client caller from the admin surface (companion of F-35) — orphaned API surface should be documented in the API map or gain a client.

### 7.9 Remaining findings

- **F-39 (LOW)** — `NotificationsViewModel` lives under `shared/.../feature/parent/presentation/` yet serves all three roles via the role-agnostic `/api/v1/notifications` route (NotificationsRouting.kt:6, :115). Misleading module ownership for a cross-role component. **Fix:** relocate to `feature/notifications/presentation/`.
- **F-42 (LOW)** — `SchoolHomeScreenV2` collects **6 separate state flows** from SchoolDashboardViewModel (adminName/progress/steps/onboardingStatus/isLoading/errorMessage) instead of one immutable state object — divergent from the single-`_state` pattern every other VM uses; risks torn reads between flows. **Fix:** consolidate into one `data class` state.
- **F-43 (LOW)** — `DiscoveryScreenV2` implements the enquiry form **twice** (sheet lines 529-538 and inline lines 647-656) with duplicated field/validation code — both currently fake (F-21). **Fix:** extract a single `EnquiryForm` composable before wiring F-21's real submit.
- **F-47 (MEDIUM)** — No `GET /school/staff/{id}` endpoint exists; `StaffProfileScreenV2` must resolve the member by scanning StaffViewModel's full roster list client-side. Fragile (depends on roster already loaded; root enabler of F-38). **Fix:** add a by-id staff fetch route + dedicated StaffProfileViewModel load, mirroring StudentProfileViewModel/TeacherProfileViewModel (RA-45 pattern).
- **F-48 (LOW)** — `MessagingCore.kt:339`: notification fallback title **"your child's school"** is parent-flavored copy applied to ALL recipient roles (teacher/admin recipients get nonsensical copy). Companion of F-51 (deepLink); split out because the fix sites differ. **Fix:** role-aware title at the `notifyMessageRecipient` call.
- **F-49 (MEDIUM)** — `SchoolCommsScreenV2` RA-49 audience targeting takes **comma-split free-text target identifiers** for CLASS/SUBJECT/STUDENT audiences — no picker, no validation against real class/student lists; a typo silently targets nobody. Q5 `announcements.audience_*` columns store whatever was typed. **Fix:** replace free-text with pickers fed by `/school/students` and class lists; server-side validate targets exist within `school_id`.
- **F-52 (LOW)** — `ResultsRouting.kt:138-145`: app_config JSON lookup wraps parsing in `.getOrElse { emptyList() }` — malformed config is silently swallowed; misconfiguration becomes invisible empty results. **Fix:** log a warning on parse failure.

---

## 8. FINDINGS INDEX (63 findings)

| ID | Sev | One-line | Location |
|---|---|---|---|
| F-01 | LOW | Brand split "VidyaSetu"/"VidyaPrayag" | SplashScreenV2:160, CommonLanding:226/688, SchoolSettings mailto |
| F-02 | MEDIUM | Dead "View All Portals" button | CommonLandingScreenV2:692-697 |
| F-03 | HIGH | False "End-to-end encrypted" claim | AuthScaffoldV2:183 |
| F-04 | MEDIUM | No OTP resend/timer | ParentAuthScreenV2 |
| F-05 | HIGH | Dead "Forgot Password?" — no reset flow exists | AdminAuthScreenV2:~117 |
| F-06 | MEDIUM | Fabricated default name "Mr. Vikram" | TeacherFirstLoginScreenV2:55 |
| F-07 | MEDIUM | Dead help link | TeacherFirstLoginScreenV2:134-140 |
| F-08 | HIGH | School match cards unselectable — first match always wins | ParentLinkChildScreenV2:204-229 + LinkChildViewModel:101 |
| F-09 | LOW | Dead VM API `selectSchool` | LinkChildViewModel.kt:77 |
| F-10 | CRITICAL | Fabricated "confirmation email sent" | SchoolOnboardingScreenV2:188-192 |
| F-11 | CRITICAL | Hardcoded seed teacher list shown as imported | SchoolOnboardingScreenV2:146-152 |
| F-12 | CRITICAL | Fake CSV import stats — no upload/parse exists | SchoolOnboardingScreenV2:716-725 |
| F-13 | CRITICAL | Hardcoded completion stats | SchoolOnboardingScreenV2:793-816 |
| F-14 | HIGH | 3 dead onboarding buttons | SchoolOnboardingScreenV2:657/713/824 |
| F-15 | MEDIUM | Dead "Message the school →" | ParentFeesScreenV2:166-170 |
| F-16 | MEDIUM | Free-text leave dates | ParentLeaveScreenV2:122-136 |
| F-17 | MEDIUM | 2 dead parent-profile rows | ParentProfileScreenV2:164-165 |
| F-18 | MEDIUM | Scholarships error has onRetry=null | ParentScholarshipsScreenV2:98 |
| F-19 | MEDIUM | UnauthRoute.Discovery unreachable | NavGraphV2 |
| F-20 | HIGH | UnauthRoute.SchoolOnboarding unreachable (whole wizard dead) | NavGraphV2 |
| F-21 | CRITICAL | Discovery enquiry submit is FAKE (no API call) | DiscoveryScreenV2:529-538, 647-656 |
| F-22 | MEDIUM | Dead Discovery filter chips | DiscoveryScreenV2:193-196 |
| F-23 | MEDIUM | Dead ♡/Compare buttons | DiscoveryScreenV2:403-404 |
| F-24 | LOW | EventRow chevron without onClick | AcademicCalendarScreenV2:225 |
| F-25 | HIGH | SriPreview fabricated labeled metrics (unsanitized vs RA-S10/S11) | SriPreview.kt:41-48, 59, 68 |
| F-26 | MEDIUM | Dead Edit icon on classes | TeacherClassesScreenV2:174 |
| F-27 | MEDIUM | Dead "Assign new homework" entry | TeacherHomeworkScreenV2:55 |
| F-28 | MEDIUM | 3 dead teacher-profile rows | TeacherProfileScreenV2 (teacher):140-155 |
| F-29 | LOW | onStudentSearch ignores its String param | SchoolPeopleScreenV2:106 |
| F-30 | MEDIUM | 5 Coming-Soon rows render chevron with onClick=null | SchoolSettingsScreenV2:172-179, 206 |
| F-31 | MEDIUM | Approve/Reject lack per-row loading (double-submit) | LeaveRequestsScreenV2:195-216 |
| F-32 | MEDIUM | Free-text announcement date | SchoolCommsScreenV2:371-378 |
| F-33 | MEDIUM | CRM funnel buttons lack per-action loading | AdmissionsCrmScreenV2 |
| F-34 | MEDIUM | Row-level Remove contradicts RA-S17 profile-only deletion | StudentRosterScreenV2:177-184 |
| F-35 | HIGH | "Publish" screen is read-only; POST /school/results orphaned | ResultsPublishScreenV2 + ResultsRouting.kt:321 |
| F-36 | LOW | Duplicated inset modifiers (double padding) | EditSchoolProfileScreenV2:58-60 vs 111-113 |
| F-37 | MEDIUM | Free-text PTM date | SchedulePtmScreenV2:124 |
| F-38 | HIGH | StaffProfile auto-onRemoved masks load errors | StaffProfileScreenV2:60-62 |
| F-39 | LOW | NotificationsViewModel misplaced under feature/parent | shared/.../parent/presentation/NotificationsViewModel.kt |
| F-40 | CRITICAL | /school/staff 500s in prod (table missing) — client face of F-53 | SchoolPeopleScreenV2 Staff tab |
| F-41 | HIGH | Hardcoded badge=1 on teacher portal | TeacherPortalV2:93 |
| F-42 | LOW | SchoolDashboardViewModel exposes 6 separate flows | SchoolHomeScreenV2 |
| F-43 | LOW | Duplicated enquiry form implementation | DiscoveryScreenV2:529-538 vs 647-656 |
| F-44 | MEDIUM | Dead notification-prefs footer | NotificationsScreenV2:284 |
| F-45 | HIGH | Scholarships overlay unreachable | ParentPortalV2 |
| F-46 | HIGH | Calendar overlay unreachable | ParentPortalV2 |
| F-47 | MEDIUM | No by-id staff fetch; profile scans roster list | StaffProfileScreenV2 + server |
| F-48 | LOW | Parent-flavored fallback notify title for all roles | MessagingCore.kt:339 |
| F-49 | MEDIUM | Audience targets are unvalidated comma-split free text | SchoolCommsScreenV2 (RA-49 dialog) |
| F-50 | LOW | `.filter { true }` no-op | Notify.kt:41 |
| F-51 | MEDIUM | deepLink hardcoded "parent/messages" for all recipient roles | MessagingCore.kt:343 |
| F-52 | LOW | Silent `.getOrElse { emptyList() }` config parse | ResultsRouting.kt:138-145 |
| F-53 | CRITICAL | NonTeachingStaffTable has NO CREATE TABLE in SQL | Tables.kt:883 vs scripts/*.sql |
| F-54 | CRITICAL | Admissions pipeline broken end-to-end (fake intake → real CRM) | DiscoveryScreenV2 ↔ AdmissionsCrmScreenV2 |
| F-55 | MEDIUM | Pattern: free-text dates ×3 (F-16/32/37) | 3 screens |
| F-56 | MEDIUM | Pattern: missing per-row action loading ×2 (F-31/33) | 2 screens |
| F-57 | MEDIUM | Pattern: destructive-action UX split (F-34) | roster vs profiles |
| F-58 | MEDIUM | Teacher-name fallback ownership check (same-name cross-access) | TeacherAccess.kt:126-128, 161 |
| F-59 | LOW | Discovery screens consumed from parent portal — placement | screens/discovery/ |
| F-60 | LOW | Stale KDoc: `/parent/notifications` vs real role-agnostic route | NotificationsViewModel.kt:34 |
| F-61 | LOW | Duplicated local date helpers | AcademicCalendarScreenV2 |
| F-62 | MEDIUM | OTP funnel fragility (resend + recovery copy) | ParentAuthScreenV2 |
| F-63 | LOW | Orphaned publish endpoint undocumented | ResultsRouting.kt:321 |

**Totals:** CRITICAL 7 · HIGH 11 · MEDIUM 26 · LOW 19 = **63**

---

## 9. FIX EXECUTION ORDER

### Tier 0 — BLOCKERS (before any production traffic)
1. **F-53/F-40** — Write `scripts/schema-patch-2026-06-11.sql` creating `non_teaching_staff` exactly matching Tables.kt:883; update README-RUN-ORDER.md; run on Supabase. *(server SQL only, zero client risk)*
2. **F-21/F-54** — Implement `POST /api/v1/admissions/enquiries` + client API method + wire both Discovery forms (extract shared form per F-43 while in there). Restores the CRM pipeline.
3. **F-10–F-14/F-20** — Decide SchoolOnboarding's fate: delete route+screen (fastest safe path) or finish it for real. Either kills the entire fabrication cluster in one move.

### Tier 1 — HIGH (before public launch)
4. **F-38 + F-47** — Add error check to StaffProfile auto-close; add `GET /school/staff/{id}` + dedicated VM load.
5. **F-08/F-09** — Wire match-card onClick → `selectSchool`; keep auto-select for single match.
6. **F-41** — Bind teacher badge to real notifications unreadCount.
7. **F-25** — Sanitize SriPreview to RA-S10/S11 standard.
8. **F-45/F-46** — Add entry points for Scholarships + Calendar overlays.
9. **F-35/F-63** — Rename ResultsPublish → Results, or add a real publish action against POST /school/results; document the endpoint.
10. **F-03** — Correct the encryption claim copy.
11. **F-05** — Implement password reset or remove the link.

### Tier 2 — MEDIUM (first stabilization sprint)
12. **F-55** — Build shared `VDatePicker`; replace 3 free-text date fields (F-16/32/37).
13. **F-56** — Extract `actingIds` helper; apply to LeaveRequests (F-31) + CRM funnel (F-33).
14. **F-58** — Backfill teacher_id, drop name fallback in TeacherAccess.
15. **F-49** — Audience-target pickers + server-side validation.
16. **F-51/F-48** — Role-aware deepLink + title in MessagingCore notify.
17. **F-34/F-57** — Remove roster row-level deletes.
18. Dead-element batch: F-02, F-04/F-62, F-07, F-15, F-17, F-22, F-23, F-26, F-27, F-28, F-30, F-44; F-18 (onRetry); F-19 (route).

### Tier 3 — LOW (opportunistic)
19. F-01 (brand constant), F-06, F-09, F-24, F-29, F-36, F-39, F-42, F-43 (done in Tier 0 if #2 lands), F-50, F-52, F-59, F-60, F-61.

---

## 10. DEFINITION OF DONE

The audit's findings are considered resolved when ALL of the following hold:

1. **Schema parity:** every Exposed table in `Tables.kt` has a matching `CREATE TABLE` in `scripts/` run order; `non_teaching_staff` exists in Supabase; `GET /school/staff` returns 200 on a fresh prod database.
2. **No fabricated data:** zero screens render hardcoded numbers/names/emails as if real — verified by grepping the F-10..F-13, F-25, F-06, F-41 sites.
3. **Pipeline integrity:** a Discovery enquiry submitted by a parent appears in AdmissionsCrmScreenV2's funnel (row in `admission_enquiries` with correct `school_id`).
4. **No dead affordances:** every Button/clickable row/chevron either performs its action or is visually marked Coming Soon without a navigation affordance (F-02/05/07/14/15/17/22/23/24/26/27/28/30/44 sites re-checked).
5. **Reachability:** no overlay enum or UnauthRoute exists without at least one navigation entry point (F-19/20/45/46).
6. **Tenancy & ownership:** TeacherAccess ownership resolves by teacher_id only (F-58); all guard-protected routes unchanged-verified (21 files).
7. **Double-submit safety:** all mutating per-row actions disable + show loading while in flight (F-31/33 pattern).
8. **Notification correctness:** deepLink and copy are role-appropriate for every recipient (F-48/51); spine no-op removed (F-50).
9. **Date integrity:** all date inputs use VDatePicker; no free-text date strings reach the API (F-55).
10. **Re-audit:** a follow-up static pass confirms each of the 63 findings closed with file:line evidence, and HARDLINE-2's six-question trace passes for every previously-failing element.

---

*End of audit — 63 findings · 7 CRITICAL · 11 HIGH · 26 MEDIUM · 19 LOW. Static analysis only; no builds executed.*
