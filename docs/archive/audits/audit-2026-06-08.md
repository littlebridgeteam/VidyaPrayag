# VidyaPrayag — REAL GOD MODE Full-System Audit + Remediation

**Date:** 2026-06-08
**Branch:** `backend-by-abuzar`
**Predecessor:** `AUDIT_2026-06-07.md` (findings A–M + sweeps A–E + deep-pass RA-01…RA-19)
**Method:** Static analysis only — the module is too large to build in the sandbox; the human builds locally. Every claim below is backed by a `file:line` citation produced via `grep`/`sed`/file reads. No Gradle was run.
**ID continuation:** The previous audit's deepest pass ended at **RA-19**. This audit continues the sequence at **RA-20**. Letter findings (A–M) and RA-01…RA-19 are *not* re-numbered; where this audit re-verifies one of them the original ID is referenced.

**Severity legend:** 🔴 CRITICAL (blocks launch / data loss / security / core flow broken) · 🟠 HIGH (feature broken, fake, or unreachable) · 🟡 MEDIUM (degraded UX / fragile) · 🟢 LOW (cosmetic / hygiene)

---

## 0. EXECUTIVE SUMMARY — what actually happens if you ship today

Compared with the 06-07 audit, the **server multi-tenancy core is now sound** (`requireSchoolContext()` reads `school_id` from the DB, never the client; WhatsApp fan-out is school-scoped at every step — RA-04 verified closed) and the **Parent Fees crash (finding D) is fixed** (server `ParentFeesResponse` now matches the client `FeeData` field-for-field). Session persistence (finding F) and logout server-revocation (§3.6) are wired.

**But the following remain broken and are the subject of this audit's remediation:**

| # | Finding | Sev | Net effect |
|---|---------|-----|------------|
| RA-20 | **Parent `/link-child` never sets `app_users.profile_completed = true`** | 🔴 | A parent who links a child still logs in to the **account-setup wizard every single time** — never reaches the dashboard. Violates the DoD line "Parent login reaches dashboard without hitting account setup." |
| RA-21 | **Logout has no confirmation dialog anywhere** (parent/teacher/admin) | 🔴 | Single tap on "Log out" destroys the session instantly. Violates LAW "ZERO confirmation-less destructive actions." |
| RA-22 | **People tab is cohort-analytics, not a teacher/student roster**; teacher provisioning endpoints are orphaned (no client) and there is **no DELETE teacher** route | 🟠 | DoD requires real teacher/student lists filtered by school + add/delete teacher writing to DB. None of that is reachable from the app. |
| RA-23 | **Admin cannot send an announcement from the app** — `POST /announcements` + client `createAnnouncement()` + VM method all exist, but the Comms screen exposes **no compose UI** | 🟠 | The single most important school write action has a complete backend and a dead UI. |
| RA-24 | **"Coming Soon" shown where the backend already exists** — SchoolHome PEWS / Live-Campus-Metrics, Comms Messages/PTM, Settings "Teacher management" | 🟠 | Violates LAW "ZERO Coming Soon where ViewModel + route already exist." |
| RA-25 | Fee `currency` defaults to `"USD"` for an India-first product | 🟡 | Parent Fees renders `$` instead of `₹` until a record overrides it. |
| RA-26 | Parent Fees announcements come from a **global** `app_config` key, not school-scoped | 🟡 | Every parent of every school sees the same fee announcement copy. |

This audit fixes RA-20 and RA-21 first (TIER 0), then the HIGH findings (TIER 2), then MEDIUM (TIER 3).

---

## DOMAIN 1 — AUTHENTICATION & SESSION

### 1.1 Session persistence — what is written vs what is read (verify finding F)
- **Write** (`AuthRepositoryImpl.saveSession`, lines 80-84): `userToken`, `userRole`, `userId`, `refreshToken`, `profileCompleted`. ✅
- **Read on cold start** (`getSession`, lines 90-102): same 5 fields reconstructed from `PreferenceRepository`. ✅ — **except `name`, which is hard-set to `""`** (line 99). The display name is intentionally *not* persisted; each portal re-fetches it from the live token (SchoolHome→`/user/details`, TeacherHome→`/home`, ParentProfile→profile API). This is acceptable: the name always comes from the **currently-logged-in user's** token, never a stale cache.
- **`PreferenceManager` (roomMain)** persists exactly those 5 keys + theme (lines 13-18). The written-set == read-set. ✅ **Finding F verified closed.**

### 1.2 Role isolation at logout (verify §3 family)
- `clearSession()` (`PreferenceManager.kt:99-107`) removes **all 5** session keys (`USER_ROLE`, `USER_TOKEN`, `USER_ID`, `REFRESH_TOKEN`, `PROFILE_COMPLETED`). `AuthRepositoryImpl.logout()` also nulls the in-memory `cachedSession` (line 127). ✅ **No session field from user A survives into user B's login.** When admin logs out and teacher logs in, the teacher's `/user/details` and `/home` calls use the *teacher's* fresh token → teacher name + teacher school only. **Verified closed.**

### 1.3 RA-20 🔴 Parent `/link-child` never flips `profile_completed`
- **Where:** `server/.../feature/parent/ParentLinkRouting.kt:121-212` (`POST /link-child`). The handler upserts the `ChildrenTable` row (lines 158-192) and returns the linked child — but **never updates `AppUsersTable.profileCompleted`** for the parent `uid`. Grep confirms: `grep -n profileCompleted ParentLinkRouting.kt` → **zero hits**.
- **Why it matters:** Parent signup sets `profileCompleted = false` (`AuthRouting.kt:366`). The client gate (`NavGraphV2.AuthedFlow`, lines 185-188) routes `EntryRole.Parent` to `ParentLinkChild` whenever `profileCompleted == false`. Because linking never flips the flag, **on the next login the server's `/login` returns `profile_completed=false` again** (`AuthRouting.kt:467`) → the parent is dumped back into the link wizard forever.
- **Contrast:** the admin path *does* flip it (`OnboardingRouting.kt:568-571`, REVIEW final submit). Parent is the only role missing the flip.
- **Fix (TIER 0):** in the `Linked` branch of `/link-child`, `AppUsersTable.update({ id eq uid }) { it[profileCompleted] = true }` inside the same `dbQuery` transaction.

### 1.4 RA-21 🔴 Logout is confirmation-less in all three portals
- **Where:**
  - Parent: `ParentProfileScreenV2.kt:132` — `VButton("Log out", onClick = onLogout …)` fires immediately.
  - Teacher: `TeacherProfileScreenV2.kt:118` — same.
  - Admin: `SchoolSettingsScreenV2.kt:142` — `SettingRow(… "Account" … onClick = onLogout)` fires immediately.
- Grep for `confirm`/`dialog` near logout → **zero hits**. There is no `AlertDialog`/confirmation anywhere in the logout path.
- **Why it matters:** LAW: "ZERO confirmation-less destructive actions (logout, delete, remove)."
- **Fix (TIER 0):** introduce a reusable confirm dialog and gate every `onLogout` behind it.

### 1.5 Parent auth flow routing (verify the DoD parent line)
- `ParentAuthScreenV2` pins role to `PARENT` (line 50), drives phone→OTP via `AuthViewModel` (`onContinue`→`checkUser`→`sendOtp`; `onSubmit`→`login`/`signup`). On success → `onAuthSuccess()` → `NavGraphV2` recomposes into `AuthedFlow`.
- The flow is correct **once RA-20 is fixed**: a returning, already-linked parent has `profile_completed=true` → routed straight to `ParentPortalV2`. The *only* reason a parent currently hits account setup on every login is RA-20.

### 1.6 Token scoping (verify)
- JWT carries only `sub`/`role`/`name` (`JwtConfig.kt:89-98`) — **no `school_id` claim**. This is *intentional and safe*: every school query resolves `school_id` from the DB via `resolveSchoolAndRole()` (`SchoolAccess.kt:56-61`), so a forged/stale claim cannot widen access. ✅
- JWT secret hard-fails in prod if unset/default (`JwtConfig.kt:60-70`) — finding E verified closed. Passwords use PBKDF2 via `PasswordHasher` with legacy self-heal (`AuthRouting.kt:177, 414-427`) — §3.3 verified closed.

### 1.7 Server-side logout/revocation (verify §3.6)
- `POST /api/v1/auth/logout` (`AuthRouting.kt:518-545`) is JWT-guarded and revokes the matching (or all) `UserSessionsTable` rows. Client `AuthApi.logout()` (lines 89-97) calls it best-effort before clearing prefs (`AuthRepositoryImpl.logout` lines 119-129). ✅ **Verified closed.**

---

## DOMAIN 2 — DATA FLOW COMPLETENESS (per feature, per role)

### SCHOOL ADMIN

| Feature | Chain status | Evidence |
|---|---|---|
| Dashboard onboarding hero (greeting + progress + steps) | ✅ LIVE | `SchoolDashboardViewModel.refresh()` → `/user/details`; `adminName` from `data.personalDetails.name` (`SchoolDashboardViewModel.kt:124`). |
| Dashboard "Live campus metrics" | 🟠 **Coming Soon despite backend** | `SchoolHomeScreenV2.kt:182` `VComingSoon` — but `SchoolAnalyticsRouting` `/overview`, `/class-performance`, `/syllabus-coverage`, `/student-cohort` all exist with real aggregates (RA-24). |
| Dashboard "Early-warning radar (PEWS)" | 🟠 **Coming Soon despite backend** | `SchoolHomeScreenV2.kt:190` `VComingSoon` + `PewsPreview`. `/student-cohort` returns real `at_risk_students` + risk counts (RA-24). |
| People tab — Teachers | 🟠 **Missing** | `SchoolPeopleScreenV2` renders cohort analytics (`StudentAnalyticsViewModel`), not a teacher roster. `GET /api/v1/school/teachers` exists (`TeacherProvisioningRouting.kt:135`) but **no client calls it** (RA-22). |
| People tab — Students | 🟠 **Missing roster** | Same screen; no flat student list/profile. Only risk analytics. |
| Teacher profile open | 🟠 **Missing** | No navigation to a single-teacher profile; no client route. |
| Add teacher | 🟠 **Missing UI/client** | `POST /api/v1/school/teachers` exists, IDOR-safe (`ctx.schoolId`), but no client API/VM/UI (RA-22). |
| Delete teacher | 🔴 **Missing route** | No `DELETE` on `/api/v1/school/teachers/{id}` anywhere on the server (RA-22). |
| Announcements — read/search | ✅ LIVE | `SchoolAnnouncementsViewModel.loadAnnouncements/searchAnnouncements`; `GET /announcements` + `/search`. |
| Announcements — **send** | 🟠 **No UI** | `createAnnouncement` exists in `AnnouncementsApi.kt:59`, VM `SchoolAnnouncementsViewModel.createAnnouncement` (line 150), server `POST` (`AnnouncementRouting.kt:173`) — but `SchoolCommsScreenV2` exposes **no compose button** (RA-23). |
| Comms — Messages / PTM | 🟠 **Coming Soon despite backend** | `SchoolCommsScreenV2.kt:113-121` `VComingSoon`; server has `MessagesRouting`, `PtmRouting` (RA-24). |
| Records — Coverage | ✅ LIVE | `SyllabusCoverageViewModel` → `/syllabus-coverage`. |
| Records — Attendance/Marks/Fee/Documents | 🟡 Coming Soon (no dedicated school-records read endpoint) | `SchoolRecordsScreenV2.kt:91-103`. These are honest gaps — no specific roll-up endpoint exists. |
| Institutional profile edit | ✅ LIVE (write) | `OnboardingRouting` `syncSchoolBasics` writes `SchoolsTable` (lines 237-253); REVIEW flips `profile_completed`. |
| Settings — Teacher management | 🟠 **Coming Soon despite backend** | `SchoolSettingsScreenV2.kt:138` text "(Coming Soon)" — backend exists (RA-24). |
| Settings — Academic year / Classes / Fee / Notifications / Data export | 🟡 Honest Coming Soon | No backend yet for these specific surfaces. |
| Logout | 🔴 No confirmation | RA-21. |

### TEACHER
- Dashboard, Class roster, **Attendance marking (write)**, **Marks entry (write)**, Homework, Syllabus, Profile edit, Notifications — **all wired** to `TeacherViewModel`/`TeacherRepository` with real submit paths. `TeacherRoutingTasks.kt` upserts `AttendanceRecordsTable` (line 210) and assessments. Teacher screens carry loading/error/empty via `VStateHost`. ✅
- The teacher surface is reachable **only after** RA-22 lets an admin create a teacher (provisioning route exists server-side; client entry missing).

### PARENT
- Dashboard (`TrackProgressViewModel`), Attendance/Marks/Syllabus/Report — **rendered as `VComingSoon`** in `ParentAcademicsScreenV2.kt:91-103`. `ParentDashboardRouting` `/dashboard` returns rich real per-child data but is **orphaned** (finding J, still unused). 🟡 carried.
- Fees — ✅ LIVE & crash-free (finding D closed); scoped by `parentId` (no IDOR). `currency` default `USD` (RA-25); announcements global (RA-26).
- Announcements/notifications — ✅ LIVE, scoped to the parent's children's schools (`ParentRouting.kt`).
- Nearby schools (Discovery) — ✅ LIVE from `SchoolsTable` with Haversine sort.
- Link child — ✅ write works, **but does not flip `profile_completed`** (RA-20).

---

## DOMAIN 3 — MULTI-TENANCY & DATA ISOLATION

- **Core guard is correct.** `requireSchoolContext()` (`SchoolAccess.kt:78-94`) resolves `school_id` + `role` from `app_users` by JWT `sub`, never from the request body/query. Every school route audited (`teacherProvisioningRouting`, `announcementRouting`, `schoolAnalyticsRouting`, `onboardingRouting`) begins with `val ctx = call.requireSchoolContext() ?: return@…`.
- **WhatsApp fan-out (RA-04) verified closed:** `sync-whatsapp` constrains every announcement id to `ctx.schoolId`, rejects foreign ids with 403, and `resolveRecipientPhones()` filters parents/children by `schoolId` at every branch (`AnnouncementRouting.kt:383-446`).
- **Parent fees (`/fees`)** scoped by `parentId eq uid` — no cross-parent leak (`ParentFeesRouting.kt:89`).
- **`/link-child` (RA-03)**: resolves the canonical student by `(schoolId, rollNumber, isActive)` (`ParentLinkRouting.kt:142-148`); a parent cannot link a child from another school by id alone, but **roll-number enumeration within a chosen school is still possible** (no rate limit). Carried as 🟡 (RA-03, documented previously). No data leak — only existence-probing.
- **No CRITICAL cross-school read leak found in this pass.** RA-22's missing DELETE route, when added, MUST scope by `ctx.schoolId` to avoid introducing an IDOR.

---

## DOMAIN 4 — HARDCODING DETECTION

- **No `MockV2` in any live render path** — confirmed: the 06-07 SWEEP-A eradication holds; remaining `MockV2` refs are `@Preview`-only.
- **No fabricated humans** in analytics — finding G holds; `Seed.kt` grep for the 8 fictional names → zero.
- **Hardcoded fee announcement fallback** (`ParentFeesRouting.kt:112-118`) — a static `"Deadline"/"2h ago"` list used only when the CMS key is absent. Combined with RA-26 (global key) this is the only fee-screen hardcoding. 🟡
- **`currency = "USD"` default** (RA-25). 🟡
- Dashboard metrics: the SchoolHome numbers shown are **real** (onboarding progress) or **honestly Coming Soon** (metrics/PEWS) — no fake numbers rendered.

---

## DOMAIN 5 — WRITE PATH COMPLETENESS

| Write action | Status | Break point |
|---|---|---|
| Parent link child | ⚠️ writes link but **not `profile_completed`** | RA-20 (server) |
| Admin send announcement | ❌ **no UI** | RA-23 (client UI only — API+VM+route all exist) |
| Admin add teacher | ❌ **no client** | RA-22 (server route exists) |
| Admin delete teacher | ❌ **no route** | RA-22 (server route missing) |
| Teacher mark attendance | ✅ complete (`submit` → upsert) | — |
| Teacher enter marks | ✅ complete | — |
| Admin institutional profile edit | ✅ complete (onboarding sync) | — |
| Admin onboarding completion | ✅ complete (flips `profile_completed`) | — |

---

## DOMAIN 6 — NOTIFICATION & REAL-TIME PROPAGATION

- **Announcement create** (`POST /announcements`) inserts the row (`AnnouncementRouting.kt:201-214`). The WhatsApp fan-out is a **separate, explicit** `POST /sync-whatsapp` step (admin-triggered), not automatic on create. That is acceptable but means "send announcement → notify" is a **two-call** flow; the client compose UI added in RA-23 should optionally trigger the sync. Fan-out is school-scoped (Domain 3). ✅ for tenancy; 🟡 for "create does not auto-notify."
- **Attendance/marks** marking does not fire any parent notification today — documented gap, not in this audit's DoD; carried.

---

## FINDINGS COUNT (this audit)

| Sev | IDs | Count |
|---|---|---|
| 🔴 | RA-20, RA-21 | 2 |
| 🟠 | RA-22, RA-23, RA-24 | 3 |
| 🟡 | RA-25, RA-26 | 2 |
| **Total new** | | **7** |

Re-verified closed from prior audit: D (fees crash), E (jwt/password), F (session), §3.6 (logout revoke), G (fake humans), RA-04 (fan-out tenancy), RA-05 (scholarship DDL), K (avatar=logout).

---

## REMEDIATION PLAN (executed in tiers below this line)

- **TIER 0 (auth):** RA-20 (server flip `profile_completed`), RA-21 (logout confirmation dialog, all 3 portals).
- **TIER 1 (critical):** RA-20, RA-21 are the only 🔴; both in TIER 0. No further 🔴.
- **TIER 2 (high):** RA-22 (teacher roster + add + DELETE, all school-scoped), RA-23 (announcement compose UI), RA-24 (wire Coming-Soon where backend exists).
- **TIER 3 (medium):** RA-25 (`INR` default), RA-26 (school-scoped fee announcements).

---

## REMEDIATION LOG

_(commit hashes appended as each finding is fixed)_

### TIER 1 — 🔴 CRITICAL  (ALL RESOLVED ✅)
- ✅ **RA-20** — `f63234f` — `fix(server): RA-20` — parent `/link-child` now flips `app_users.profile_completed=true` inside the same `dbQuery`, so returning parents reach the dashboard instead of the link wizard.
- ✅ **RA-21** — `f5bc396` — `fix(ui): RA-21` — new `VConfirmDialog` primitive (composed from frozen V* primitives, zero new tokens) gates `onLogout` in Parent/Teacher/Admin profile+settings screens. All three retain loading/error/empty via `VStateHost`.
- ✅ **RA-27 / RA-34** — `f9c5cb4` — `fix(server): RA-34/RA-27` — `is_active` now enforced at `/login` (reject before credential check), `/refresh` (reject + revoke all sessions = kill-switch), `requireSchoolContext()`, `requireTeacherContext()`, and the JWT `validate{}` block (instant revocation for ALL roles incl. parent).
- ✅ **RA-33** — `78d2c81` — `fix(server): RA-33` — `OTP_DEV_RETURN_CODE` default flipped to `false` and hard-gated to non-production (force-suppressed whenever `DATABASE_URL` is set), killing the prod OTP-echo account-takeover primitive. `.env.example` updated.

### TIER 2 — 🟠 HIGH  (ALL RESOLVED ✅)
- ✅ **RA-35** — `511979e` — `fix(server): RA-35` — `/refresh` now **rotates** the refresh token: mints a new token, revokes the presented session row, and inserts a new session row carrying the same device metadata; returns the new token. **Reuse detection**: presenting an already-revoked refresh token revokes the entire session family (`REFRESH_REUSE_DETECTED`). Client already persists the returned `refresh_token` (`Koin.kt:115-118`) — no client change needed.
- ✅ **RA-36** — `033172f` — `fix(server): RA-36` — global `intercept(ApplicationCallPipeline.Plugins)` rejects any non-multipart body over `MAX_JSON_BODY_BYTES` (1 MB) with `413 PAYLOAD_TOO_LARGE`, closing the unbounded-`receive` OOM/DoS vector on public JSON routes (multipart still capped separately at 25 MB by `MediaRouting`).
- ✅ **RA-37** — `dca3cd3` — `fix(server): RA-37` — CORS `anyHost()` gated behind `!isProduction` (or no configured origins); in production the allow-list is built from `CORS_ALLOWED_ORIGINS` via `allowHost(host, schemes=…)`. `.env.example` documents the variable.
- ✅ **RA-22** — `fe1ac73` — `fix(server+client): RA-22` — teacher roster made real end-to-end: server `GET` filtered to active teachers, new IDOR-safe `DELETE /{id}` soft-delete (school-scoped, revokes sessions, ties into RA-34 kill-switch); new client stack (DTOs, `TeachersApi`, `TeachersRepository(+Impl)`, `SchoolTeachersViewModel`, Koin registrations); `SchoolPeopleScreenV2` roster section with loading/error/empty via `VStateHost` (RULE-7), add-teacher dialog, and per-row remove gated by `VConfirmDialog`. Frozen V* primitives only.
- ✅ **RA-23** — `8cf5b2d` — `fix(ui): RA-23` — admin can now compose & send announcements from `SchoolCommsScreenV2`: a "New announcement" `VButton` (above `VStateHost`, so it works on the empty state) opens a new `ComposeAnnouncementDialog` (category chips via the existing `FilterChip`, title/date/message `VInput`s) that posts via `SchoolAnnouncementsViewModel.createAnnouncement` and dismisses only on server success, then re-fetches the list. Frozen V* primitives only.
- ✅ **RA-24** — `2ead270` — `fix(ui): RA-24` — removed Coming-Soon where a backend + screen already exist. SchoolHome "Live campus metrics" → `AnalyticsDashboardScreenV2`; "PEWS" → People-tab cohort (real `/student-cohort`); Comms "Messages"/"PTM" → `MessagesScreenV2`/`SchedulePtmScreenV2`; Settings "Teacher management" → People-tab roster (RA-22). Each `VComingSoon` replaced by a tappable entry card (frozen V* primitives, teal/tealDeep tokens, `VIcons.ChevronRight`); navigation routed via new `SchoolPortalV2` callbacks (tab switch / overlay push). Notifications tab keeps `VComingSoon` (no backend yet).
- ✅ **RA-31** — `725d5fd` — `fix(server+client): RA-31` — parent dashboard now supports multiple children. Server `DashboardResponse` carries a `children: []` of ALL active children (`child_summary` kept = first for back-compat); client `ParentDashboardData` gains `children`; `ParentHomeViewModel` exposes `children`/`selectedChildId`/`selectChild()` with `childSummary` derived from the selection (falls back to first, preserves selection across refresh, falls back to `child_summary` for older servers); `ParentHomeScreenV2` renders a horizontal `ChildChip` switcher when 2+ children are linked. Frozen V* primitives only.

### TIER 3 — 🟡 MEDIUM  (ALL RESOLVED ✅)
- ✅ **RA-25** — `d83b0a8` — `fix(server): RA-25` — fee currency defaults to INR (₹) for an India-first product. `ParentFeesRouting.money()` else-branch → ₹, the no-record fallback → `"INR"`; `ParentDashboardRouting.formatMoney()` else-branch → ₹. Matches the `FeeRecordsTable.currency` column default (already INR). Legitimate USD→$ mappings untouched.
- ✅ **RA-26** — `8b830c2` — `fix(server): RA-26` — parent fee announcements are now school-scoped. `ParentFeesRouting` resolves the parent's school from their first active child (`ChildrenTable.schoolId`) and prefers CMS key `parent_fees_announcements:<schoolId>`, falling back to the legacy global key and then the static default — so each school publishes its own copy without breaking existing global configs or school-less parents.
- ✅ **RA-29** — `a8eeb14` — `fix(client): RA-29` — single Authorization source + no stale session cache. (1) Stripped the redundant manual `header(Authorization,"Bearer $token")` from every API client (Admin/Auth/Parent/Teacher/Schools, ~60 call sites across 17 files) — the Ktor `Auth` bearer plugin on the shared `HttpClient` is now the sole header attacher, so the double-wired path that could disagree with the plugin's `loadTokens` read is gone. (2) `AuthRepositoryImpl` dropped the write-once in-memory `cachedSession`; `getSession()` now always reads `prefs` (the same store the plugin's `refreshTokens` rewrites on a 401), so a rotated token is reflected immediately instead of a stale pre-rotation token surviving until cold start. Cleaned now-unused `HttpHeaders`/`header` imports and collapsed the empty builder lambdas; `token` params retained to avoid cascading signature churn through repositories/viewmodels.
- ✅ **RA-28** — `725fda1` — `fix(client): RA-28` — web session now persists. New `LocalStoragePreferenceManager` (jsMain + wasmJsMain) backs `PreferenceRepository` with `window.localStorage` (keys namespaced `vp.*`), seeding reactive StateFlows from storage at construction so a browser reload no longer drops token/role/userId/refreshToken. jsMain uses `kotlinx.browser.localStorage` + `org.w3c.dom.get/set`; wasmJsMain (no `kotlinx.browser`) uses three tiny `js(...)` bridges — no new Gradle dependency. Both `PlatformModule` DI bindings switched from `InMemoryPreferenceManager` to `LocalStoragePreferenceManager`; theme is preserved across `clearSession()` to match the mobile DataStore.
- ✅ **RA-30** — `203875e` — `fix(client): RA-30` — robust server error-message extraction. `NetworkResult.safeApiCall` replaced the fragile `substringAfter("\"message\":\"")` parsing (which broke on escaped quotes, `"message" : "..."` spacing, pretty-printed JSON, or a null message) with a new `extractErrorMessage(errorBody, statusCode)` helper that lenient-deserializes the canonical `{success,message,data}` envelope (`ErrorEnvelope` projection, `ignoreUnknownKeys`+`isLenient`), prefers `message` then `error`, and falls back to the trimmed body or `"Server returned <status>"`. Non-JSON bodies and blank bodies degrade gracefully.
- ✅ **RA-32** — `341fd67` — `fix(school): RA-32` — teacher credential reset. New server `POST /api/v1/school/teachers/{id}/reset-password` (`TeacherProvisioningRouting.kt`) generates a fresh high-entropy password via `SecureRandom` (12 chars, ambiguity-free alphabet, ~69 bits), persists ONLY the hash, marks email verified, revokes the teacher's live `UserSessionsTable` rows, and returns the plaintext ONCE in `TeacherCredentialDto`. IDOR-safe — every lookup/update constrained to `ctx.schoolId` + role `teacher` + `isActive`; phone-only (OTP) teachers get `409 TEACHER_USES_OTP`. Cross-layer client wiring in the same commit (RULE-4): `TeacherCredentialDto`, `TeachersApi.resetTeacherPassword` (Auth-plugin bearer, consistent with RA-29), `TeachersRepository`/`Impl.resetTeacherPassword`, and `SchoolTeachersViewModel.resetPassword` + `dismissIssuedCredential` exposing the one-time credential via `SchoolTeachersState.issuedCredential`.
- ✅ **RA-38** — `0df5590` — `fix(server): RA-38` — OTP send/verify are now transactional. In `OtpService`, the resend-limit check + UPSERT (`send()`) and the row load + attempt-count increment/lock (`verify()`) each previously spanned **two** separate `dbQuery{}` transactions, leaving a TOCTOU window where concurrent requests could read the same counter and both write — exceeding `maxResendsPerHour`/`maxAttempts`. Each check-then-act is now collapsed into **one** `dbQuery{}` transaction, and the target `auth_otps` row is read with `SELECT … FOR UPDATE` (`forUpdate(ForUpdateOption.ForUpdate)`), so concurrent senders/verifiers on the same `(identifier, purpose)` row serialise on the row lock. Root-cause fix — no retry/catch wrapper; no call-site changes (signatures unchanged).
- ✅ **RA-39** — `d38ce70` — `fix(server): RA-39` — admin-vs-staff RBAC. `SchoolAccess.kt` gains `SCHOOL_ADMIN_ROLES = {school_admin, admin}` and `requireSchoolAdmin()` — it delegates to `requireSchoolContext()` (same DB role read + `is_active`/onboarding checks) then rejects any role not in `SCHOOL_ADMIN_ROLES` with `403 ADMIN_REQUIRED`, so `school_staff` can no longer perform privileged writes. Applied to the privileged writes the finding names: `TeacherProvisioningRouting` (POST create, DELETE, POST reset-password), `MediaRouting` (DELETE), `AnnouncementRouting` (POST create + POST sync-whatsapp). Reads and shared writes (teacher list, media upload, announcement list/search) keep `requireSchoolContext()`. Role still read from DB, so a forged/stale claim cannot widen access.
- ✅ **RA-41** — `f842c29` — `fix(server): RA-41` — login enumeration + password throttle. `POST /auth/login` no longer distinguishes unknown-account from wrong-password on the email path: both return one generic `401 INVALID_CREDENTIALS` ("Invalid email or password"), so the response can't enumerate accounts (the prior `USER_NOT_FOUND` branch is gone from `/login`). New `LoginThrottle` (in-memory sliding window, no new deps / no schema change) throttles the password path per-IP **and** per-identifier (default 8 failures / 15 min, env `LOGIN_MAX_ATTEMPTS`/`LOGIN_WINDOW_SECONDS`), returning `429 LOGIN_THROTTLED`; failures are recorded on both unknown-account and wrong-password, and a successful login clears the identifier window. Phone/OTP path unchanged (already throttled by `OtpService`). No client changes.

### TIER 4 — 🟢 LOW  (ALL RESOLVED ✅)
- ✅ **RA-40** — `a412abc` — `fix(server): RA-40` — `DEBUG_ERRORS` leak + stderr-only logging. `ErrorHandling.kt`'s `Throwable` handler now logs through SLF4J (`errorLog.error("Unhandled error on {}", uri, cause)`) — full stack trace via the configured logback appender and the hook point for a Sentry SLF4J appender — replacing raw `System.err.println` + `cause.printStackTrace()`. `DEBUG_ERRORS` (raw exception → client) is now opt-in **and** hard-gated to non-production: suppressed whenever `DATABASE_URL` is set, even if `DEBUG_ERRORS=true`. `.env.example` documents `DEBUG_ERRORS` (never true in prod) plus the RA-41 `LOGIN_*` tunables.

---
---

# 🔁 RE-AUDIT — ROUND 2 (deeper pass) — 2026-06-08

> This section is **additive**. Round 1 (above) covered the six prescribed domains and surfaced RA-20…RA-26. This deeper pass re-read the layers Round 1 under-weighted: **the auth *guard* (not just login), the HTTP/DI network layer, per-platform persistence, the error envelope, route-registration completeness, and per-child data fan-out.** IDs continue at **RA-27**. None duplicate RA-20…RA-26.
>
> Several Round-1 *carried* items from the 06-07 audit are **re-verified CLOSED** here (good news, recorded for honesty): finding **J** (ParentDashboard orphan) is now consumed by a dedicated `ParentHomeViewModel`; finding **SHAREDVM** (Home/Academics sharing `TrackProgressViewModel`) is resolved — `ParentHomeScreenV2` now injects its own `ParentHomeViewModel`; finding **I** (media upload had no `authenticate("jwt")`) is fixed — `MediaRouting.kt:83` wraps the routes; the client HTTP `install(Auth){ bearer { refreshTokens {…} } }` now exists (`Koin.kt:88-122`), so §3.4 auto-refresh is live (not dead code).

## RE-AUDIT ROUND-2 executive summary

| ID | Title | Sev | Class |
|---|---|:--:|---|
| RA-27 | **`is_active` is never enforced at login OR in the JWT guard** — a deactivated/expelled user can still log in and keep using a live token | 🔴 | auth / security |
| RA-28 | **Web (js / wasmJs) session is in-memory only** — a browser reload silently logs the user out | 🟡 | persistence (web target) |
| RA-29 | Ktor `Auth` bearer plugin **and** manual `Authorization: Bearer $token` headers are **both** set on most authenticated calls; on a 401-refresh the plugin updates `prefs` but **not** `AuthRepositoryImpl.cachedSession` | 🟡 | network correctness |
| RA-30 | `safeApiCall` error-message extraction is **fragile string-splitting** (`substringAfter("\"message\":\"")`) — breaks on escaped quotes or `"message" : "..."` spacing → users get a wrong/blank error | 🟡 | error handling |
| RA-31 | **Parent dashboard renders only the *first* active child** — a parent with 2+ linked children sees one child and has no switcher | 🟠 | data completeness |
| RA-32 | Teacher provisioning has **no "download/initial credentials" return path beyond the create response**, and **no resend / password-reset** — an admin who closes the create dialog can never recover the teacher's initial password | 🟡 | feature gap |

**Tally (Round 2):** 6 new — 1 🔴, 1 🟠, 4 🟡.
**Combined this audit (Round 1 + 2):** RA-20…RA-32 = **13 findings** — 3 🔴, 4 🟠, 6 🟡.

---

## RA-27 🔴 `is_active` is never enforced (login + guard)
- **Login** (`AuthRouting.kt` `/login`, lines ~408-471): after `lookupUserByIdentifier` the handler verifies password/OTP and issues a token — it **never reads `AppUsersTable.isActive`**. Grep: `grep -n isActive AuthRouting.kt` → the **only** hit is line 367, which *sets* `isActive=true` at signup. There is no `if (!row[isActive]) fail(...)` anywhere.
- **Guard** (`SecurityModule.kt:31-33`): the JWT `validate { }` block only checks the audience claim and wraps a `JWTPrincipal` — it does **not** re-read the DB to confirm the user is still active. `requireSchoolContext()` (`SchoolAccess.kt:78-94`) reads `school_id`+`role` but **not** `is_active`.
- **Net effect:** Deactivating a user (`is_active=false`) does **nothing** — they can still log in fresh, and any already-issued token (up to 1-day access / 30-day refresh) keeps working. There is no kill-switch for a compromised or off-boarded account. This is the most serious *new* finding.
- **Fix (later):** (a) reject login when `!row[isActive]`; (b) in the JWT `validate` block (or a thin per-request check in `requireSchoolContext`/`principalUserUuid`), reject when the user row is inactive; (c) optionally revoke that user's `UserSessionsTable` rows on deactivation.

## RA-28 🟡 Web session is in-memory only
- **Where:** `shared/src/jsMain/.../InMemoryPreferenceManager.kt` and `wasmJsMain/.../InMemoryPreferenceManager.kt` back `PreferenceRepository` with `MutableStateFlow` only — nothing touches `localStorage`/`IndexedDB`. `clearSession()` resets the in-memory flows.
- **Where it's bound:** `roomMain` (DataStore, persistent) is `dependsOn`'d only by `androidMain`, `jvmMain`, `iosArm64Main`, `iosSimulatorArm64Main` (`shared/build.gradle.kts:69-77`). **js and wasmJs are NOT** → they fall back to the in-memory manager.
- **Net effect:** On Android + iOS (the shipping mobile targets) session persists correctly. On **web**, a page reload drops token/role/userId/refreshToken → the user is bounced to the landing funnel. Acceptable only if web is dev-only; if web is a launch target this is a 🔴. Filed 🟡 pending confirmation of web's launch status.
- **✅ RESOLVED** `725fda1` — `LocalStoragePreferenceManager` (jsMain + wasmJsMain) persists all session fields to `window.localStorage`; DI bindings switched off the in-memory manager. Reloads now retain the session on web.

## RA-29 🟡 Double Authorization header + cache staleness on refresh
- **Double header:** the global `HttpClient` installs `Auth { bearer { loadTokens {…} } }` (`Koin.kt:88-94`) which attaches `Authorization: Bearer <access>` to **every** request. Yet most API clients *also* set the header manually — e.g. `AuthApi.getUserDetails` (`AuthApi.kt:69`), every `ParentApi` method (`header("Authorization","Bearer $token")`), `AnnouncementsApi`, etc. The two paths can disagree (manual token from a VM-read pref vs the plugin's `loadTokens` read), and the plugin's `sendWithoutRequest`/precedence is implicit. Result is fragile and makes the auto-refresh path partially moot for clients that hardcode the header.
- **Cache staleness:** the plugin's `refreshTokens` block writes the new access/refresh to `prefs` (`Koin.kt:117-118`) but does **not** update `AuthRepositoryImpl.cachedSession`. Any code path that later calls `getSession()` returns the **stale in-memory** token until the next cold start, even though `prefs` are fresh.
- **Net effect:** Works today (token-bearing calls succeed) but is double-wired and inconsistent; a future change to either path can silently send the wrong/old token. **Fix:** pick ONE source of truth — let the `Auth` plugin own the header and strip the manual `header("Authorization", …)` from every API client; and have `refreshTokens` also refresh `cachedSession` (or drop the in-memory cache entirely and always read prefs).
- **✅ RESOLVED** `a8eeb14` — manual bearer headers stripped from all 17 API clients (plugin is now the sole attacher); `cachedSession` deleted so `getSession()` always reads the prefs the plugin keeps fresh. Both defects fixed at the root.

## RA-30 🟡 Fragile server-error message extraction
- **Where:** `NetworkResult.kt:86-91`. On a non-2xx, the message is pulled via `errorBody.substringAfter("\"message\":\"").substringBefore("\"")`. This breaks when: the message contains an escaped quote (`\"`), the JSON is pretty-printed with spaces (`"message" : "…"`), or `message` is null. The user then sees a truncated/blank error instead of the server's real reason (e.g. "Too many OTP requests").
- **Fix:** deserialize the standard `{success,message,data}` envelope with `kotlinx.serialization` (lenient) and read `.message`, falling back to the status line only on parse failure.
- **✅ RESOLVED** `203875e` — added `extractErrorMessage(errorBody, statusCode)` (lenient `ErrorEnvelope` deserialize → `message` then `error` → trimmed body → `"Server returned <status>"`); `safeApiCall` now calls it instead of `substringAfter`.

## RA-31 🟠 Parent dashboard shows only the first child
- **Where:** `ParentDashboardRouting.kt:151-155` — `ChildrenTable … where parentId == uid AND isActive … orderBy createdAt ASC … .firstOrNull()`. Only the earliest-linked active child is returned; `child_summary` is a single object, and there is **no child-list / child-switcher** anywhere in the parent UI.
- **Net effect:** A parent with two or more children at the same (or different) school sees exactly one. The others' progress/attendance/fees are unreachable from the dashboard. `is_active` *is* respected here (so RA-14's is_active worry is closed), but **multi-child** is a genuine gap.
- **Fix:** return a `children: []` array (and let the client render a selector), or at minimum document single-child as an intentional MVP limit.

## RA-32 🟡 No teacher-credential recovery
- **Where:** `TeacherProvisioningRouting.kt` returns the new teacher's identifier in the `POST` response (lines 121-131) but there is **no** endpoint to (a) re-display/download the initial credential later, nor (b) reset/resend a teacher's password or trigger a teacher OTP from the admin side.
- **Net effect:** Once the admin dismisses the create-teacher result, the only recovery is a manual DB edit. The Settings copy literally promises "download credentials" (`SchoolSettingsScreenV2.kt:138`).
- **Fix:** add an admin-scoped `POST /api/v1/school/teachers/{id}/reset-password` (re-issues an initial password, school-scoped) and surface the credential in the add-teacher flow added by RA-22.
- **✅ RESOLVED** `341fd67` — endpoint added (SecureRandom password, hash-only persist, session revoke, school-scoped IDOR-safe, OTP teachers → 409) and surfaced through the full client stack (`TeacherCredentialDto` → API → repo → `SchoolTeachersViewModel.resetPassword`/`issuedCredential`).

---

## Cross-cutting verification notes (Round 2)

- **Route registration is complete** — `Application.kt:147-189` mounts all 25+ route groups including `teacherProvisioningRouting()` (line 184). No orphaned *server* route except the documented two-call announce→sync-whatsapp flow.
- **Multi-tenancy re-confirmed sound** — every school route opens with `requireSchoolContext()`; `school_id` always derived from DB. No new IDOR found. When RA-22's DELETE is added it MUST scope by `ctx.schoolId`.
- **No new `MockV2` in live paths**; the `@Preview` seeds remain the only references.
- **JWT secret / password hashing / logout-revocation** remain closed (findings E, §3.3, §3.6).

*End of RE-AUDIT Round 2. 6 new findings (RA-27…RA-32). No fixes applied in this pass — audit only, per instruction. Remediation deferred.*

---
---

# 🔁🔁 RE-AUDIT — ROUND 3 (deepest pass — security/infra layer) — 2026-06-08

> This section is **additive**. Rounds 1–2 covered feature/data-flow completeness, the auth *guard*, the network/DI layer, per-platform persistence and per-child fan-out. This **third pass deliberately ignored features** and instead drilled the **security & infrastructure substrate** that the first two rounds skimmed: **CORS / engine hardening, request-body DoS surface, OTP transactional integrity & race windows, the dev-mode OTP echo default, refresh-token rotation/reuse, role-claim enforcement vs. role-from-DB, `is_active` propagation across *all* auth entry points, and error/PII leakage.** IDs continue at **RA-33**. None duplicate RA-20…RA-32.

## Round 3 findings index

| ID | Title | Sev | Domain |
|----|-------|-----|--------|
| RA-33 | **`OTP_DEV_RETURN_CODE` defaults to `"true"`** — if the env var is unset in production the API **echoes the plaintext OTP in the `/send-otp` response body** (`dev_code`), a complete OTP/auth bypass for anyone who can call the public endpoint | 🔴 | auth / config-default |
| RA-34 | **`is_active` is not enforced at `/login`, `/refresh`, OR in `requireSchoolContext()`** — extends RA-27: a deactivated user not only keeps a live token, they can also **mint brand-new tokens via `/refresh`** and pass every school guard. `is_active` is *written* at signup (AuthRouting:367) and **never read** anywhere in auth/core | 🔴 | auth / security |
| RA-35 | **Refresh tokens are never rotated** — `/refresh` returns the *same* `refresh_token` and only bumps `last_used_at` (AuthRouting:497-505). A leaked refresh token is replayable for its full 30-day life with no detection of concurrent use (no rotation = no theft signal) | 🟠 | auth / token hygiene |
| RA-36 | **No request-body size limit on JSON endpoints** — only `MediaRouting` caps bytes (25 MB). Every other `call.receive<…>()` will buffer an arbitrarily large body into memory → trivial OOM DoS on the 512 MB Render dyno via a single fat POST to e.g. `/api/v1/auth/login` | 🟠 | infra / DoS |
| RA-37 | **CORS is `anyHost()` with `Authorization` allowed** in all environments — any website can script authenticated cross-origin calls with a bearer token it has obtained. Acceptable for dev; must be locked to known origins in prod | 🟠 | infra / CORS |
| RA-38 | **OTP send & verify run as a sequence of separate `dbQuery{}` blocks, not one transaction** — the rate-limit "read resend_count → compare → update" (OtpService:156-191) and the verify "read attempt_count → compare → update" (OtpService:337-365) are **TOCTOU races**. Concurrent requests can exceed `maxResendsPerHour` and `maxAttempts`, weakening the brute-force lock | 🟡 | concurrency / auth |
| RA-39 | **`role` JWT claim is decorative — never used for authorization** — `principalRole()` is defined but **zero call sites** use it to gate access. Authorization relies entirely on role-from-DB inside `requireSchoolContext()`/`TeacherAccess`. This is *actually safer* (RA-style: stale claim can't widen access) **but** it means **`school_staff` and `school_admin` are indistinguishable** on every school route — no admin-only vs staff-only separation (e.g. teacher provisioning, media delete) | 🟡 | authz / RBAC granularity |
| RA-40 | **`DEBUG_ERRORS=true` leaks raw exception messages to clients** — gated correctly (default safe) but if ever flipped on in prod it returns `cause.message` (may contain SQL fragments / internal paths). Also the catch-all always `printStackTrace()` to stderr — fine, but pair with a real logger/Sentry before launch | 🟢 | observability / leakage |
| RA-41 | **Login user-enumeration & no per-IP login throttle** — `/login` returns distinct `USER_NOT_FOUND` vs `INVALID_CREDENTIALS` (AuthRouting:409 vs 415), letting an attacker enumerate valid accounts; and password login has **no attempt throttle** (only the OTP path is rate-limited). Combined with RA-36 this is a credential-stuffing-friendly surface | 🟡 | auth / enumeration |

**Combined this audit (Rounds 1 + 2 + 3):** RA-20…RA-41 = **22 findings** — 5 🔴, 7 🟠, 9 🟡, 1 🟢.

---

## RA-33 🔴 `OTP_DEV_RETURN_CODE` defaults to `"true"` → OTP echoed in prod
- **Where:** `OtpService.kt:125-127` — `env("OTP_DEV_RETURN_CODE", "true").equals("true", true)`. The **default is `"true"`**, so unless DevOps explicitly sets `OTP_DEV_RETURN_CODE=false`, the value flows into `OtpSendResult.Sent.devCode` → serialized as `dev_code` in `SendOtpResponse` (AuthRouting:89, 251).
- **Net effect:** Any anonymous caller hits public `POST /api/v1/auth/send-otp { identifier }` and **reads the OTP straight back in the JSON**, then logs in / signs up as that phone number. This is a full pre-auth account-takeover primitive if the env isn't overridden.
- **Fix (later):** flip the default to `"false"` and tie "echo allowed" to `!isProduction` (mirror `JwtConfig.isProduction` = `DATABASE_URL` present). In prod the field must always be `null`.

## RA-34 🔴 `is_active` not enforced at login / refresh / school guard
- **Where:** written at `AuthRouting.kt:367` (`it[isActive] = true`); **read nowhere** in `feature/auth/**` or `core/**` (grep confirms only TeacherAccess uses `isActive`, and that's the *assignment* table, not the user). `/login` (408-471), `/refresh` (474-512), and `requireSchoolContext()` (SchoolAccess:78-94) all ignore it.
- **Net effect:** Deactivating/expelling a user has **no effect** — they keep their access token, can **mint fresh tokens via `/refresh`**, and pass every school guard. Supersedes/extends RA-27 by adding the `/refresh` and guard entry points.
- **Fix (later):** in `/login` reject inactive before issuing; in `/refresh` re-load the user and reject if `!isActive`; in `requireSchoolContext()` (and a parallel parent/teacher guard) treat `!isActive` as 403. Optionally re-check in JWT `validate{}` for instant revocation.

## RA-35 🟠 Refresh tokens are never rotated
- **Where:** `AuthRouting.kt:497-505` — on refresh, only `lastUsedAt` is updated and `req.refreshToken` is returned **unchanged**.
- **Net effect:** No rotation ⇒ no reuse-detection. A stolen refresh token is valid for the entire 30-day window with the legitimate user none the wiser (both can use it indefinitely).
- **Fix (later):** issue a NEW refresh token on every `/refresh`, revoke the old session row, and persist the new hash. Detect reuse of a revoked refresh token as a theft signal → revoke the whole family.

## RA-36 🟠 No request-body size limit on JSON endpoints
- **Where:** `Application.kt` installs no body/size limit; only `MediaRouting.kt:73` caps bytes (multipart, 25 MB). All other `call.receive<…>()` buffer unbounded.
- **Net effect:** A single large POST (e.g. to public `/login` or `/signup`) can OOM the dyno → DoS.
- **Fix (later):** install a global max-content-length guard (Ktor `RequestValidation`/plugin or a `intercept(ApplicationCallPipeline.Plugins)` that rejects `Content-Length` over N), e.g. 1 MB for JSON routes.

## RA-37 🟠 CORS `anyHost()` + `Authorization` in all envs
- **Where:** `Application.kt:109-122`.
- **Net effect:** Open CORS is fine for dev but in prod any origin can drive authenticated requests with a captured bearer token.
- **Fix (later):** gate `anyHost()` behind `!isProduction`; in prod allow only the known web/app origins via `allowHost(...)`.

## RA-38 🟡 OTP send/verify are multi-statement, not transactional (TOCTOU)
- **Where:** `OtpService.send()` resend-limit check (156-191) and `verify()` attempt-count update (337-365) each span **separate** `dbQuery{}` calls.
- **Net effect:** Concurrent requests interleave between the read and the update, so `maxResendsPerHour`/`maxAttempts` can be exceeded — the brute-force lock is weaker than advertised.
- **Fix (later):** wrap each check-then-update in a single `dbQuery {}` transaction (one `transaction` block) and/or use a `SELECT … FOR UPDATE` row lock on the `auth_otps` row.
- **✅ RESOLVED** `0df5590` — both `send()` and `verify()` now do the check-then-act in a single `dbQuery{}` with `forUpdate(ForUpdateOption.ForUpdate)` locking the `auth_otps` row, serialising concurrent callers on `(identifier, purpose)`.

## RA-39 🟡 `role` claim never enforced; staff == admin on school routes
- **Where:** `SecurityModule.kt:49` defines `principalRole()` — **0 authorization call sites** (grep). Authz is role-from-DB in `requireSchoolContext()` (SchoolAccess:85, `SCHOOL_ROLES = {school_admin, school_staff, admin}`).
- **Net effect:** Reading role from DB is the *correct* call (a forged/stale claim can't widen access — keep it). **But** there's no admin-vs-staff granularity: any `school_staff` row can provision teachers, delete media, send school-wide announcements, etc.
- **Fix (later):** introduce `requireSchoolAdmin()` (role ∈ {school_admin, admin}) for privileged writes (teacher provisioning, media delete, announcement create) and keep `requireSchoolContext()` for reads/shared writes.
- **✅ RESOLVED** `d38ce70` — `requireSchoolAdmin()` + `SCHOOL_ADMIN_ROLES` added and applied to teacher provisioning (create/delete/reset-password), media delete, and announcement create + sync-whatsapp; reads/shared writes keep `requireSchoolContext()`.

## RA-40 🟢 `DEBUG_ERRORS` leak + stderr-only logging
- **Where:** `ErrorHandling.kt:57-62`. Default is safe ("Something went wrong"); flipping `DEBUG_ERRORS=true` returns `cause.message` to clients.
- **Fix (later):** keep default; document that `DEBUG_ERRORS` must never be set in prod; route `printStackTrace()` through SLF4J/Sentry.
- **✅ RESOLVED** `a412abc` — `ErrorHandling.kt` logs via SLF4J (`errorLog.error(…, cause)`, Sentry-appender-ready) instead of stderr/`printStackTrace`; `DEBUG_ERRORS` hard-gated to non-production (suppressed when `DATABASE_URL` set) and documented in `.env.example`.

## RA-41 🟡 User enumeration + no password-login throttle
- **Where:** `/login` distinguishes `USER_NOT_FOUND` (409) from `INVALID_CREDENTIALS` (415); password path has no rate limit (only OTP path does, via OtpService).
- **Net effect:** Account enumeration + unthrottled credential stuffing on the email/password path.
- **Fix (later):** return a single generic "Invalid credentials" for both branches; add a per-IP / per-identifier login attempt throttle (reuse the OTP rate-limit pattern or a simple sliding-window table).
- **✅ RESOLVED** `f842c29` — generic `INVALID_CREDENTIALS` for both unknown-account and wrong-password on the email path; new `LoginThrottle` sliding-window per-IP + per-identifier (429 `LOGIN_THROTTLED`).

---

## Cross-cutting verification notes (Round 3)

- **OTP core is otherwise strong** — per-row salt + global pepper, SHA-256, constant-time compare (`constantTimeEquals`, OtpService:401-406), 10-min TTL enforced in code **and** DB, resend cap, lockout. The weaknesses are the **default echo (RA-33)** and the **non-transactional race (RA-38)**, not the crypto.
- **Password hashing confirmed sound** — PBKDF2 via `PasswordHasher`, salted, with transparent legacy-hash upgrade on login (AuthRouting:418-427). Finding E stays closed.
- **JWT secret hard-fail in prod confirmed** (`JwtConfig.secret`, 60-70). Doc/code drift noted: header comment says "7 days" but `expirySecs` default is now `86400` (1 day) — code is correct, comment stale (cosmetic, not a finding).
- **Media upload hardened** — 25 MB cap, MIME allow-list, school-scoped path & delete (MediaRouting). No path-traversal (object path derived server-side).
- **Analytics `student/{studentId}` is school-scoped** (`StudentsTable.id eq id AND schoolId eq ctx.schoolId`, SchoolAnalyticsRouting:700) — no cross-school IDOR. Messages threads scoped by `ownerUserId AND schoolId` (MessagesRouting) — sound.
- **Multi-tenancy remains sound across the board** — no new cross-tenant read/write found in Round 3.

*End of RE-AUDIT Round 3. 9 new findings (RA-33…RA-41). No fixes applied in this pass — audit only, per instruction. Remediation deferred ("we will fix this later").*

---
---

## REVERIFICATION — 2026-06-08

> Per the GOD-MODE reverification mandate: every finding marked ✅ RESOLVED in the
> remediation log above was re-opened at the exact `file:line` cited in its fix
> commit and re-confirmed **in the current `backend-by-abuzar` working tree** — not
> from the commit message. Each entry below records the live evidence (grep / file
> read) and confirms no new defect was introduced at the changed callsite(s).
> **Method:** static analysis only (no Gradle). 21 closed findings reverified.

### TIER 0 / TIER 1 — 🔴 CRITICAL

[RA-20] — ✅ CONFIRMED
File: server/.../feature/parent/ParentLinkRouting.kt:198-199
Evidence: the `Linked` branch now runs `AppUsersTable.update({ AppUsersTable.id eq uid }) { it[profileCompleted] = true }` inside the same `dbQuery`. A returning, already-linked parent's `/login` now reports `profile_completed=true`, so `NavGraphV2.AuthedFlow` routes straight to `ParentPortalV2` instead of the link wizard. No other callsite of the link handler regressed.

[RA-21] — ✅ CONFIRMED
File: components/VStructure.kt:171 (`VConfirmDialog`); parent/ParentProfileScreenV2.kt:83,151; teacher/TeacherProfileScreenV2.kt:75,137; school/SchoolSettingsScreenV2.kt:85,166
Evidence: `VConfirmDialog` is a real `Dialog`+`VCard` primitive (frozen V* tokens, `VButtonVariant.Destructive` confirm). All three portals trigger it via `onClick = { showLogoutConfirm = true }` and only call `onLogout()` inside `onConfirm`. No path fires logout directly. Built from existing tokens — RULE-1 honoured.

[RA-27 / RA-34] — ✅ CONFIRMED
File: AuthRouting.kt:442 (login), AuthRouting.kt:552 (refresh + session revoke); core/SchoolAccess.kt:97 (guard); core/SecurityModule.kt:36-50 (JWT `validate{}`)
Evidence: `is_active` is read and rejected at all four auth entry points. Login rejects before the credential check (`ACCOUNT_DEACTIVATED`); refresh rejects AND revokes all `UserSessionsTable` rows (kill-switch); `requireSchoolContext()` reads the full row once and rejects inactive; the JWT validate block re-reads `isActive` per request and returns `null` (instant revocation for ALL roles incl. parent). Grep confirms the only `it[isActive] = true` write is the signup default (line 367).

[RA-33] — ✅ CONFIRMED
File: feature/auth/OtpService.kt:138
Evidence: `devReturnCode = !isProduction && env("OTP_DEV_RETURN_CODE", "false").equals("true", true)` — default flipped to `false` AND hard-gated to non-production (`isProduction` = `DATABASE_URL` present). `dev_code` (line 288) is null in prod even if the env var is set true. The pre-auth OTP-echo takeover primitive is closed.

### TIER 2 — 🟠 HIGH

[RA-35] — ✅ CONFIRMED
File: AuthRouting.kt:530-539 (reuse detection), 565-573 (rotation)
Evidence: `/refresh` mints `newRefresh = JwtConfig.issueRefreshToken(...)`, revokes the presented session row and inserts a new row; presenting an already-revoked-but-unexpired token revokes the entire family (`REFRESH_REUSE_DETECTED`). Client already persists the returned token — no client change needed, none regressed.

[RA-36] — ✅ CONFIRMED
File: Application.kt:107,123-132
Evidence: `MAX_JSON_BODY_BYTES = 1 MB`; a global `intercept(ApplicationCallPipeline.Plugins)` rejects non-multipart bodies over the cap with `413 PAYLOAD_TOO_LARGE`. Multipart still capped separately by `MediaRouting` (25 MB). OOM/DoS vector closed.

[RA-37] — ✅ CONFIRMED
File: Application.kt:145-165
Evidence: in production (`DATABASE_URL` set) with `CORS_ALLOWED_ORIGINS` configured, CORS is built from an explicit `allowHost(host, schemes)` allow-list; `anyHost()` only on dev/local or prod-without-allow-list. `Authorization` is still allowed but no longer to any origin in prod.

[RA-22] — ✅ CONFIRMED
File: TeachersApi.kt:43 (`getTeachers`), 59-63 (`deleteTeacher` → `DELETE api/v1/school/teachers/{id}`); SchoolPeopleScreenV2.kt:34-37,51,226 (roster + `VConfirmDialog` remove)
Evidence: real client stack (Api/Repo/Impl/VM/Koin) exists; roster section renders with `VStateHost` loading/error/empty and an add-teacher dialog; per-row remove gated by `VConfirmDialog`. Server DELETE is school-scoped soft-delete that revokes sessions (ties into RA-34 kill-switch).

[RA-23] — ✅ CONFIRMED
File: SchoolCommsScreenV2.kt:180 ("New announcement" button), 191 + 251 (`ComposeAnnouncementDialog`), 74 (`viewModel.createAnnouncement`)
Evidence: the compose button sits above `VStateHost` (works on empty state); the dialog posts via `SchoolAnnouncementsViewModel.createAnnouncement` and dismisses only on server success, then re-fetches. Frozen V* primitives only.

[RA-24] — ✅ CONFIRMED
File: SchoolHomeScreenV2.kt:221 (`MetricEntryCard` replaces metrics/PEWS ComingSoon); SchoolCommsScreenV2.kt:140-150 (`CommsEntryCard` Messages/PTM); SchoolSettingsScreenV2.kt:162 ("Teacher management" → `onOpenTeachers`)
Evidence: every named target now routes to a real backend-backed screen via `SchoolPortalV2` callbacks. The only surviving `VComingSoon` are Comms→Notifications (no backend — documented) and the Records sub-tabs (out of RA-24's named scope, no backend yet). No fabricated data introduced.

[RA-31] — ✅ CONFIRMED
File: ParentFeatureModels.kt:21 + ParentHomeViewModel.kt:22-33,70-74 (`children`/`selectedChildId`/`childSummary` derived); ParentHomeScreenV2.kt:65,111-113,226 (`ChildChip` switcher)
Evidence: server returns `children: []`; VM exposes selection, preserves it across refresh, and falls back to `child_summary` for old servers. UI renders a horizontal switcher when 2+ children are linked.

### TIER 3 — 🟡 MEDIUM

[RA-25] — ✅ CONFIRMED
File: ParentFeesRouting.kt:79,82,102; ParentDashboardRouting.kt:126-128
Evidence: `money()` maps `INR → ₹` and the else-branch defaults to `₹`; the no-record fallback currency is `"INR"`. Legitimate `USD → $` mapping untouched.

[RA-26] — ✅ CONFIRMED
File: ParentFeesRouting.kt:119-131
Evidence: the handler resolves the parent's school from their first active child (`ChildrenTable.schoolId`) and prefers CMS key `parent_fees_announcements:<schoolId>`, falling back to the legacy global key then the static default. School-less parents and old configs still work.

[RA-29] — ✅ CONFIRMED
File: shared/src/commonMain (all 17 API clients)
Evidence: grep for `header("Authorization"` / `Bearer $token` across `commonMain` returns ZERO real callsites — the only hit is a doc comment in `OnboardingModels.kt:16`. The Ktor `Auth` bearer plugin is the sole header attacher; `cachedSession` is gone so `getSession()` always reads fresh prefs.

[RA-28] — ✅ CONFIRMED
File: shared/src/jsMain/.../prefs/LocalStoragePreferenceManager.kt + shared/src/wasmJsMain/.../prefs/LocalStoragePreferenceManager.kt
Evidence: both web targets back `PreferenceRepository` with `window.localStorage` (namespaced keys) and seed StateFlows at construction; DI switched off the in-memory manager. A browser reload retains the session.

[RA-30] — ✅ CONFIRMED
File: NetworkResult.kt:37 (`extractErrorMessage`), 117 (callsite)
Evidence: `safeApiCall` now calls `extractErrorMessage(errorBody, statusCode)` (lenient envelope deserialize → `message` then `error` → trimmed body → `"Server returned <status>"`). The fragile `substringAfter` is gone (only referenced in the explanatory comment at line 33).

[RA-32] — ✅ CONFIRMED
File: TeachersApi.kt:67 (`resetTeacherPassword`), TeachersRepository(.Impl) + SchoolTeachersViewModel + TeacherModels.kt:41 (`TeacherCredentialDto`)
Evidence: full client stack wired to the school-scoped `POST .../{id}/reset-password`; one-time credential surfaced via `SchoolTeachersState.issuedCredential`. Server side IDOR-safe (scoped to `ctx.schoolId`, role teacher, isActive; OTP teachers → 409).

[RA-38] — ✅ CONFIRMED
File: OtpService.kt:66 (import `ForUpdateOption`), 175-186 (send) and verify path
Evidence: each check-then-act is collapsed into a single `dbQuery{}` and the `auth_otps` row is read with `forUpdate(ForUpdateOption.ForUpdate)`, so concurrent senders/verifiers on `(identifier, purpose)` serialise on the row lock. TOCTOU window closed.

[RA-39] — ✅ CONFIRMED
File: SchoolAccess.kt:46 (`SCHOOL_ADMIN_ROLES`), 127-133 (`requireSchoolAdmin` → 403 `ADMIN_REQUIRED`); applied at AnnouncementRouting.kt:175,231; MediaRouting.kt:189; TeacherProvisioningRouting.kt:105,198,243
Evidence: privileged writes (teacher create/delete/reset, media delete, announcement create + sync-whatsapp) now require an admin role read from the DB; `school_staff` is rejected. Reads/shared writes keep `requireSchoolContext()`.

[RA-41] — ✅ CONFIRMED
File: AuthRouting.kt:412-416 (throttle), 428-429 & 453-454 (generic `INVALID_CREDENTIALS`), 459 (success clears window)
Evidence: the email login path returns one generic `401 INVALID_CREDENTIALS` for both unknown-account and wrong-password (no enumeration); `LoginThrottle` throttles per-IP + per-identifier with `429 LOGIN_THROTTLED`. The surviving `USER_NOT_FOUND` (line 548) is on `/refresh`, which does not leak account existence to an attacker probing logins — not a regression.

### TIER 4 — 🟢 LOW

[RA-40] — ✅ CONFIRMED
File: core/ErrorHandling.kt:37 (`errorLog`), 46-47 (`isProduction`), 75 (SLF4J log), 80 (`DEBUG_ERRORS` gate)
Evidence: the `Throwable` handler logs via SLF4J (`errorLog.error("Unhandled error on {}", uri, cause)`) instead of `System.err`/`printStackTrace`; `DEBUG_ERRORS` is hard-gated to non-production (suppressed whenever `DATABASE_URL` is set).

### REVERIFICATION VERDICT

**21 / 21 previously-closed findings → ✅ CONFIRMED. 0 regressions. 0 partials.**
No finding returns to the fix queue. Cleared to proceed to PART 2 (Premium UI Elevation).

**Build-cache note (pre-Part-2 fix):** the upgraded `CommonLandingScreenV2`
(commit `78c33bf`) was verified correct end-to-end (NavGraphV2:122 → screen;
`LandingViewModel` registered in Koin; bundled `landing_school_*.webp` valid and
tracked) — yet a local build kept shipping the OLD landing UI. Root cause was the
Compose-Multiplatform resource generator being served stale from the Gradle build
cache (`org.gradle.caching=true`), so the generated `Res.drawable` accessors lagged
the source set. Fixed by disabling the build cache (`gradle.properties`,
commit `22c9ed0`); pair with `./gradlew clean` on first build after pulling.

---

## PART 2 — PREMIUM UI ELEVATION — COMPLETION LOG (2026-06-08)

All eight features executed, one commit each, on `backend-by-abuzar`. Every change
stays inside the existing design language (RULE-1), reuses the `V*` primitive
system (RULE-3), is commonMain-safe (RULE-5), and was held to "stability over
polish" (RULE-2).

```
F1  feat(splash): native SplashScreen API — logo exit animation, zero flash      ✅ 7368ffd
F2  feat(ui): shimmer skeleton system — ShimmerBox + all dashboard screens        ✅ 006d419
F3  feat(nav): premium dock — animated pill, spring icons, haptic, edge-to-edge   ✅ 068e486
F4  feat(nav): screen enter/exit transitions — momentum-based, role-consistent    ✅ f3ca155
F5  feat(ui): staggered list entrance + card press scale feedback                 ✅ 2949cd4
F6  feat(ui): VAvatar — image + initial fallback, deterministic colour, shimmer   ✅ d315b6f
F7  feat(ui): micro-interactions — pull-to-refresh, success morph, error shake    ✅ 18b2d94
F8  feat(ui): typography tightening, tonal elevation, hairline dividers           ✅ cdf84be
```

Plus the landing-ship build fix: `fix(build): force CMP resource re-generation +
bump versionCode so device installs fresh` (`25b7eb5`) — extends the earlier
build-cache disable by also forcing the resource-generator tasks to re-run and
bumping `versionCode 1→2` so a device can never keep a cached pre-`78c33bf` APK.

### NOTES ON DELIBERATE DEVIATIONS (RULE-2 / RULE-3)

- **F5 press scale**: existing `pressScale` (~30 callsites) was LEFT untouched; a
  new spec-aligned `cardPressScale` (0.97f, graphicsLayer, MediumBouncy) was added
  alongside it so nothing already shipped shifts.
- **F6 VAvatar**: was already the single avatar primitive at ~24 callsites, so it
  was EXTENDED in place (crossfade + circular shimmer + deterministic initial
  fallback + `VAvatarSize` constants), giving every screen the upgrade for free
  with a backward-compatible API.
- **F7 success morph**: already provided by `VButton`'s `stateful`/`loading` flow
  (idle → spinner → Check icon → idle); the form simply drives it. New code was
  only the `VPullRefresh` wrapper and the `shakeOnError` modifier.
- **F8 tonal elevation**: NOT applied on the elevation channel. The design owns a
  bespoke navy-tinted `vElevation` system, not M3 Surface `tonalElevation`; the
  only tier above resting `Card` is the 8px/24px `Raised` sheet weight, which is
  too heavy as a resting state for every navigable card and reads as distortion on
  dense screens. Trialled and reverted per RULE-2. Interactive cards are instead
  differentiated by the F5 `cardPressScale` tactile cue. `h4` letter-spacing was
  likewise left at 0 because it doubles as `VButton`'s label style.
