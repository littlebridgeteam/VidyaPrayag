# VidyaSetu (vidyaprayag) — Full System Audit
**Date:** 2026-06-09
**Branch:** `backend-by-abuzar_v1.0.2` · **HEAD:** `414a73c`
**Method:** Static analysis only (no builds, no Gradle, no sandbox runs). Every claim below is read off the **actual code on this branch** — prior audits (`AUDIT_2026-06-07.md`, `audit-2026-06-08*.md`) were treated as untrusted and re-verified from source.
**Stack:** Kotlin Compose Multiplatform (`composeApp` UI + `shared` domain/data) · Ktor server (`server`) · Supabase Postgres via Exposed · Koin DI · DataStore prefs.
**Roles:** School Admin · Teacher · Parent (multi-tenant by `school_id`).
**Findings:** 20 total (RA-S01…RA-S16 original audit; RA-S17…RA-S20 = four user-reported issues triaged in the Addendum below).

---

## Severity Legend

| Badge | Meaning |
|-------|---------|
| 🔴 **CRITICAL** | Security hole, data-integrity violation, or a flow that is outright broken / deceives the user. Fix before any release. |
| 🟠 **HIGH** | A real user-facing bug or a missing core capability that ships a degraded product. Fix this cycle. |
| 🟡 **MEDIUM** | Confusing UX, hardcoded/fabricated cosmetic data, or a partial feature. Fix soon. |
| 🟢 **LOW / OK** | Minor polish, or a verified-correct area documented so it is not re-investigated. |

**Finding shape:** every entry carries `FINDING-ID · Severity · Location · Problem · Evidence · Impact · Fix spec`.

---

## Executive Summary

The backend is in materially better shape than the UI. The **server is genuinely multi-tenant-safe** (every school-scoped route derives `school_id` and `role` from the JWT-resolved DB row, never from the request body — see DOMAIN 8) and the **teacher vertical writes real data to Supabase with correct parent notifications** (DOMAIN 6). The biggest remaining problems are on the **client / cross-role-glue** layer:

- **🔴 Session state-bleed** — the singleton Ktor `HttpClient` `Auth` bearer cache is never cleared on logout (`clearToken()` does not exist anywhere). A logout → re-login as a different role can serve stale bearer tokens from cache.
- **🔴 Fabricated student data shown in production** — `PewsPreview` (admin home) and `AiReportCardPreview` (parent academics) render hardcoded fake students/grades ("Aman Verma 9-B 78", "180 students scored") as if they were live signals.
- **🟠 Parent login asks returning parents for their NAME** *(this audit's newest reported bug)* — the OTP step unconditionally shows a "Your name" field even on `LOGIN_PHONE`, where the ViewModel ignores it entirely.
- **🟠 Notification asymmetry** — teachers notify parents on every write (attendance/marks/homework/leave/message); **admins do not notify on direct messages**, and **admin results-publish sends no notification**.
- **🟠 Reply-only messaging on parent & admin sides** — the API supports compose-new (`recipient_user_id`) but neither the parent nor the admin UI exposes a "new message / pick recipient" affordance.
- **🟡 Parent multi-child non-propagation** — each parent ViewModel holds its own `selectedChildId`; switching child on one tab does not carry to the others.
- **🟡 Cosmetic hardcoding** — hardcoded "Parent" avatar name, always-on red bell dots, a fixed Comms-tab badge of `2`, and fabricated landing social-proof ("240+ schools · 38k parents").

**Second deliverable status (premium common landing page):** **Already substantially built.** `CommonLandingScreenV2.kt` is a full premium landing surface in the V* design system, carries the legacy content forward, exposes **both** Parent and Admin login entries, and already uses the 3 bundled campus images. Only the fabricated social-proof line needs honesty cleanup. See DOMAIN 2.

### Addendum — four user-reported issues audited 2026-06-09 (RA-S17 … RA-S20)

A second round of user reports was triaged against the live code. All four are **confirmed** and added below; none change the security posture (RA-S14 still holds).

- **🟠 RA-S17 (People tab) — architecture gap, not a bug.** The admin People tab is a flat scroll with a Teacher roster + Student roster card and **a direct "Remove" button on each teacher row**. There are **no sub-tabs** (Teachers / Students / Non-teaching staff), **no non-teaching-staff feature at all** (no table, no route, no UI), **no search/filter on the tab**, and **deletion is not inside the profile** — the profiles (`StudentProfileScreenV2`, `TeacherProfileScreenV2`) have no delete affordance. User's explicit ask: sub-tabs + clickable real DB-backed profiles + search/filter + move delete (with confirm popup) into the profile and **remove the direct row button**.
- **🟠 RA-S18 (Teacher attendance "Submit" persists) — ROOT CAUSE FOUND; it is a UI bug, NOT a DB bug.** Attendance **does** persist: `TeacherRoutingTasks.kt` POST `/attendance` upserts `attendance_records` and notifies parents (verified, same path as RA-S15). The Submit button stays visible because `TeacherAttendanceScreenV2.kt` renders `VButton(stateful = true, …)`, and in `stateful` mode `VButton` runs an **internal time-based** phase machine (Idle→Loading 650 ms→Success 1400 ms→Idle) that is **completely disconnected from the network result** — it ignores both `loading = state.isSubmitting` and `state.submitSuccess`. So the button fakes a success animation and reverts to "Submit" regardless of the real outcome.
- **🟠 RA-S19 (Parents can't see attendance/marks) — data plane is wired & correct; the gap is the attendance UX + a fragile marks join.** The child-scoped read endpoints exist and are correctly joined (`/parent/child/{id}/{attendance,marks,syllabus}`, ownership-gated, registered in `Application.kt`); teacher write keys match parent read keys (`person_id == student_code`). The real defects: **(a)** attendance is rendered as a **flat list of date rows**, not the **calendar** the user demands; **(b)** marks visibility is fragile — the parent marks query filters on `section == child.section`, which **falls back to `"A"`** when the linked student row is missing/mismatched, and only shows `is_published == true` assessments, so a section mismatch or unpublished marks silently yields an empty screen that reads as "can't see marks". RA-S11 (fabricated `AiReportCardPreview` on the same screen) compounds the confusion.
- **🟡 RA-S20 (Common landing worse than demanded).** Screenshots confirm `CommonLandingScreenV2` renders but reads plain/generic versus the premium bar, and still shows the **fabricated** "Bank-grade encryption" pill + "240+ schools · 38k parents" strip (this is RA-S13's copy). Supersedes the over-optimistic RA-S16 "DONE": the surface exists but needs an honesty pass + visual polish, not a rebuild.

---

## Findings Index

| ID | Sev | Domain | One-liner |
|----|-----|--------|-----------|
| RA-S01 | 🔴 | 1 Auth | ✅ `15ede8c` Bearer-token cache never cleared on logout (state-bleed). |
| RA-S02 | 🟠 | 1 Auth | ✅ Name field at OTP step now gated on `AuthFlow.SIGNUP_PHONE` (returning parent never asked). |
| RA-S03 | 🟡 | 1 Auth | ✅ FIXED (`4066ff4`,`d2a8ac7`) — `USER_NAME` pref added to interface + all 5 impls; `saveSession` persists `name`, `getSession` reads it, `getUserDetails` refreshes it; surfaced in parent header avatar (RA-S06). `school_id` still server-derived (correct). |
| RA-S04 | 🟡 | 1 Auth/Nav | ✅ `PENDING` Parent NEVER force-linked after auth; child-linking is opt-in from Profile. |
| RA-S05 | 🟡 | 3 Parent | ✅ FIXED (`8350888`,`9d85d01`) — shared `SelectedChildHolder` (Koin single, `StateFlow<String?>`) observed/published by Home/Academics/Leave VMs; `FeeViewModel` re-fetches `getFees(token, childId)` → `GET /parent/fees?child_id=`; cleared on logout. |
| RA-S06 | 🟡 | 3 Parent | ✅ FIXED (`d2a8ac7`) — parent account avatar uses real `accountName` (RA-S03 pref); parent + admin home bell dots now render only when `NotificationsViewModel.unreadCount > 0` (no more permanent red dot). |
| RA-S07 | 🟠 | 4 Comms | ✅ `FIXED` Compose-new shipped for BOTH admin (teacher picker) & parent (child's class teacher + office picker via new `/recipients` endpoint). |
| RA-S08 | 🟠 | 4/7 Comms | ✅ Admin (and parent) DM now notifies recipient via shared `notifyMessageRecipient`; resolves peer from engine. |
| RA-S09 | 🟠 | 5/7 Admin | ✅ Admin results-publish now notifies class parents (`Notify.toUsers`, category `marks`) — teacher-path parity. |
| RA-S10 | 🔴 | 5 Admin | ✅ `23ce113` `PewsPreview` rewritten as label-free schematic (no fabricated students). |
| RA-S11 | 🔴 | 3 Parent | ✅ `7ee5271` `AiReportCardPreview` rewritten as label-free schematic (no fabricated grades). |
| RA-S12 | 🟡 | 5 Admin | ✅ FIXED (`71291b1`) — Comms `VNavItem` badge now = count of message threads with `unreadCount > 0` (via injected `MessagesViewModel`); hidden when 0. No more hardcoded `2`. |
| RA-S13 | 🟡 | 2 Landing | ✅ `FIXED` Fabricated "240+ schools · 38k parents" + "Bank-grade encryption" removed; legacy SocialProofSection carried forward. |
| RA-S14 | 🟢 | 8 Tenancy | **VERIFIED SECURE** — `school_id`/`role` always from JWT-resolved DB context. |
| RA-S15 | 🟢 | 6 Teacher | **VERIFIED GOOD** — teacher writes hit Supabase, scoped, with parent notifications. |
| RA-S16 | 🟢 | 2 Landing | **VERIFIED DONE** — premium V* landing exists with Parent+Admin entries + images. *(superseded by RA-S20 — honesty/polish still owed)* |
| RA-S17 | 🟠 | 5 Admin | ✅ FIXED — server (`93bfbde`) + shared (`eb4fece`,`b7ebd21`) + UI (`c8a918b`,`936021a`): `non_teaching_staff` table + `/api/v1/school/staff` CRUD + students `?q=&class=` search; Staff model/API/repo/VM + Koin; People rewritten to `VTopTabs` 3-sub-tabs + search + tappable rows; delete moved into Teacher/Student/Staff profiles behind `VConfirmDialog`, direct row button removed. |
| RA-S18 | 🟠 | 6 Teacher | ✅ Attendance **and** Marks Submit now result-driven (real `isSubmitting`/`submitSuccess`); `VButton` gained external `success`; inline submit errors. |
| RA-S19 | 🟠 | 3 Parent | ✅ `FIXED` Parent attendance is now a month-calendar; marks/syllabus joins relax the `section` filter when it's the `"A"` fallback (no more silent empty). |
| RA-S20 | 🟡 | 2 Landing | ✅ `FIXED` Common landing rebuilt from the legacy reference content (hero/featured/social-proof/entry-points/showcase/portals/footer) in the V* system; 3 bundled campus images used. |

---

# DOMAIN 1 — Session & Authentication

### RA-S01 · 🔴 CRITICAL · ✅ FIXED `15ede8c` (compile-corrected `448a33c`) · Bearer-token cache is never cleared on logout (session state-bleed)

> **Compile-correction (2026-06-09, `448a33c`):** the original `15ede8c` fix used `client.plugin(Auth).providers.filterIsInstance<BearerAuthProvider>().forEach { it.clearToken() }`. That does **not** compile against **Ktor 3.4.3** — the installed `Auth` plugin instance returned by `client.plugin(Auth)` does **not** expose a public `.providers` member (only `AuthConfig.providers`, on the config block, which is inaccessible after install). The unresolved `.providers` cascaded into all 3 reported errors in `:shared:compileDevDebugKotlinAndroid` (`SessionManager.kt:31` unresolved `providers`, `:33` unresolved `it`, `:29` cannot infer `runCatching` type). **Corrected to the public extension property** `client.authProviders` (`io.ktor.client.plugins.auth.authProviders`, verified against the ktor 3.4.3 source tag; `BearerAuthProvider.clearToken()` confirmed present). Behaviour and callsites unchanged.

- **Location:** `shared/src/commonMain/kotlin/com.littlebridge.enrollplus/di/Koin.kt` (HttpClient `single { … Auth { bearer { loadTokens/refreshTokens } } }`, ~lines 60–112); `shared/.../feature/auth/data/repository/AuthRepositoryImpl.kt` (`logout()` → `clearSession()`).
- **Problem:** The `HttpClient` is a Koin `single` (one instance for the process). It installs the Ktor `Auth` plugin with a `bearer { loadTokens { … }; refreshTokens { … } }` block. The Ktor `Auth`/`BearerAuthProvider` keeps an **in-memory token cache**. On logout the app calls `clearSession()` (clears DataStore) — but **never calls `Auth` provider `clearToken()`**. A repo-wide grep for `clearToken` returns **zero** hits.
- **Evidence:**
  ```
  $ grep -rn "clearToken" shared/src/commonMain/...   → (no results)
  ```
  `PreferenceRepository.clearSession()` removes `USER_ROLE / TOKEN / ID / REFRESH / PROFILE_COMPLETED` only. The bearer provider's cached `BearerTokens` survive because the singleton client is never recreated and `clearToken()` is never invoked.
- **Impact:** After logout → re-login (especially as a **different role** on the same device), the first authenticated requests can still be signed with the **previous user's cached bearer token** until a 401 forces a refresh. Cross-role data exposure and "ghost session" bugs. This is the root of the recurring "logged out but still sees old data" class of issues.
- **Fix spec:**
  1. Expose a way to clear the provider via the public **`HttpClient.authProviders`** extension property and call `.clearToken()` on logout. (Do **not** use `client.plugin(Auth).providers` — that does not compile on Ktor 3.x; see compile-correction note above.)
  2. In `AuthRepositoryImpl.logout()` (and any forced-logout path), after `clearSession()`, invoke the bearer clear. Minimal pattern:
     ```kotlin
     import io.ktor.client.plugins.auth.authProviders
     // …
     client.authProviders
         .filterIsInstance<BearerAuthProvider>()
         .forEach { provider -> provider.clearToken() }
     ```
     Wrap this in a small `SessionManager.clearAuthCache()` that `logout()` calls.
  3. Belt-and-braces: `loadTokens` must read the **current** DataStore values each call (verify it isn't capturing a stale snapshot); return `null` when no token is stored so a logged-out client never sends a token.

---

### RA-S02 · 🟠 HIGH · ✅ FIXED · Returning parent is asked for their NAME on the OTP step *(newly reported in this audit)*
- **Fix:** `ParentAuthScreenV2.kt` `AuthStep.Otp` branch now renders the "Your name" field **only when `state.flow == AuthFlow.SIGNUP_PHONE`** (new family). A returning parent (`LOGIN_PHONE`) sees only the OTP field. UI-only change — the `AuthViewModel.onSubmit()` already gated the name *requirement* and the `login()` payload on `SIGNUP_PHONE`, so this aligns the UI with the existing VM contract (no name collected/sent on login).
- **Location:** `composeApp/src/commonMain/kotlin/com.littlebridge.enrollplus/ui/v2/screens/auth/ParentAuthScreenV2.kt` — `AuthStep.Otp` branch.
- **Problem:** A parent enters their phone number; the server's `checkUser` decides the flow. Both **`LOGIN_PHONE`** (existing account) and **`SIGNUP_PHONE`** (new account) land on the same UI step, `AuthStep.Otp` (confirmed in `AuthViewModel.onContinue()`, line 67: `AuthFlow.LOGIN_PHONE, AuthFlow.SIGNUP_PHONE -> AuthStep.Otp`). The screen then **unconditionally** renders a "Your name" `VInput` above the OTP field — even for a returning user logging in.
- **Evidence:**
  ```kotlin
  // ParentAuthScreenV2.kt — AuthStep.Otp branch
  AuthStep.Otp -> {
      // Phone signup needs a name alongside the OTP; show it inline for new families.
      // (For an existing-user login the field stays empty and is ignored by the VM.)
      VInput(
          value = state.name,
          onValueChange = viewModel::onNameChanged,
          label = "Your name",                // ← shown to RETURNING users too
          placeholder = "Full name",
          leadingIcon = VIcons.User,
          modifier = Modifier.fillMaxWidth(),
      )
      Spacer(Modifier.height(16.dp))
      VInput(value = state.otp, ... label = "OTP" ...)
  }
  ```
  The ViewModel **already** does the right thing — it only requires/sends `name` when the flow is signup (`AuthViewModel.onSubmit()`, lines 125–145):
  ```kotlin
  AuthStep.Otp -> {
      if (currentState.flow == AuthFlow.SIGNUP_PHONE && currentState.name.isBlank()) { … "Please enter your name" … }
      if (currentState.flow == AuthFlow.SIGNUP_PHONE) {
          repository.signup(SignupRequest(name = currentState.name, …))
      } else {
          repository.login(LoginRequest(identifier = …, otp = …, role = …))   // name NOT sent
      }
  }
  ```
  So the field is **purely UI cruft for returning users** — collected, displayed, then ignored. The user's complaint is exactly right: *"WHEN I'M LOGGING AS PARENT WHOSE ACCOUNT HAS ALREADY [BEEN] CREATED IT STILL ASKS FOR NAME AND OTP — WHY ASK FOR NAME WHEN [IT WAS] CREATED ALREADY? JUST ASK FOR OTP."*
- **Impact:** Returning parents are confronted with a "Your name" box on every login. It looks like the app forgot who they are; it invites them to type a name that does nothing; it erodes trust in a sign-in flow that should feel instant. New families still need the field — so the fix must be flow-aware, not a blanket removal.
- **Fix spec (UI-only, no VM change needed):** Gate the name field on the signup flow inside the `AuthStep.Otp` branch:
  ```kotlin
  import com.littlebridge.enrollplus.feature.auth.domain.model.AuthFlow
  // …
  AuthStep.Otp -> {
      if (state.flow == AuthFlow.SIGNUP_PHONE) {        // ← only new families see the name field
          VInput(
              value = state.name,
              onValueChange = viewModel::onNameChanged,
              label = "Your name",
              placeholder = "Full name",
              leadingIcon = VIcons.User,
              modifier = Modifier.fillMaxWidth(),
          )
          Spacer(Modifier.height(16.dp))
      }
      VInput(value = state.otp, onValueChange = viewModel::onOtpChanged, label = "OTP", …)
      Spacer(Modifier.height(8.dp))
      Text("We sent a code to ${state.identifier.ifBlank { "your phone" }}.", …)
  }
  ```
  - `state.flow` is available on `AuthUiState.flow: AuthFlow?` and is set by `onContinue()` before the step changes.
  - Optionally tighten the subtitle copy so returning users read "Enter the code we just sent you" while new families read "Create your family account."
  - **Acceptance:** existing parent → enters phone → sees **only** the OTP field; new parent → enters phone → sees name + OTP. No VM changes required.

---

### RA-S03 · 🟡 MEDIUM · ✅ FIXED · `name` is discarded at sign-in; prefs has no `name`/`school_id` key

**Fix (`4066ff4` data layer + `d2a8ac7` surfacing):** Added a `USER_NAME` string key + `getUserName()/setUserName()` to the `PreferenceRepository` interface and **all 5 implementations** (roomMain DataStore, jsMain/wasmJsMain InMemory + LocalStorage), included in `clearSession()`. `AuthRepositoryImpl.saveSession` now persists `response.name`; `getSession()` reads it back (no longer hardcodes `name=""`); `getUserDetails()` refreshes the persisted name from `PersonalDetails.name` on success. `school_id` is deliberately **not** persisted — the server authoritatively derives it from the JWT (RA-S14 invariant). The name is surfaced in the parent header avatar via RA-S06 (`TrackProgressState.accountName`).

- **Location:** `shared/.../feature/auth/data/repository/AuthRepositoryImpl.kt` (`saveSession`, `getSession`); `shared/.../core/prefs/PreferenceRepository.kt` + roomMain `PreferenceManager.kt`.
- **Problem:** `saveSession(...)` persists `profileCompleted, userId, refreshToken, role, token` but **not the display name**. `getSession()` returns `name = ""` hardcoded. `PreferenceRepository` exposes keys for theme/role/token/id/refresh/profileCompleted only — there is **no `name` and no `school_id` key**.
- **Evidence:** `PreferenceRepository` interface (read in full) lists exactly: `getThemeName/getUserRole/getUserToken/getUserId/getRefreshToken/getProfileCompleted/clearSession` — no name, no school_id.
- **Impact:** Two downstream effects: (a) the client can never greet the user by name, which is why `ParentPortalV2` hardcodes "Parent" (RA-S06); (b) any client code wanting `school_id` must re-derive it from a network call. *(Note: NOT persisting `school_id` is actually fine for security — the server authoritatively derives it from the JWT. This is a UX/data-completeness gap, not a security one, hence MEDIUM.)*
- **Fix spec:** Add a `USER_NAME` preference key (string) + `getUserName()/setUserName()`; populate it from the login/signup response and from `GET /api/v1/user/details`. Use it for the portal headers and avatars. Do **not** trust a client-stored `school_id` for authorization — keep that server-derived.

---

### RA-S04 · 🟡 MEDIUM · ✅ FIXED `PENDING` · Parent is NEVER force-linked after auth; child-linking is opt-in from Profile
- **Location:** `composeApp/.../ui/v2/navigation/NavGraphV2.kt` (~line 197); `ParentProfileScreenV2.kt`; `ParentPortalV2.kt`.
- **PRODUCT DIRECTIVE (supersedes the original "re-enable the forced gate" fix):** Do **NOT** push a parent into the child-link flow after signup/login. A freshly authenticated parent lands **straight on their portal** and can link a child **whenever they choose, from their Profile**. This avoids dead-ending new families on a "link your child" wall before they've seen the app.
- **Fix applied:**
  1. `NavGraphV2.kt` parent gate → `EntryRole.Parent -> AuthedRoute.Portal` (no conditional child-link push). The `AuthedRoute.ParentLinkChild` route is retained but is no longer reached by the gate.
  2. `ParentProfileScreenV2.kt` gained an `onLinkChild: () -> Unit` callback; the **"Linked children"** profile row (`"Link a child or manage who you follow"`) is now the single, opt-in entry point — implemented via a `ProfileRow(title, sub, onClick)` data class rendered through `VCard(onClick = row.onClick)` (`onClick` is nullable on `VCard`, verified).
  3. `ParentPortalV2.kt` added a `ParentOverlay.LinkChild` value; the Profile overlay passes `onLinkChild = { overlay = ParentOverlay.LinkChild }`, and the new overlay renders `ParentLinkChildScreenV2(onDone = back-to-tabs, onBack = back-to-Profile)`. Predictive-back already dismisses overlays to the tabs.
- **Result:** Onboarding no longer dead-ends; linking is a deliberate, reversible action the parent initiates from Profile (the server still creates a PENDING admin-approval request, unchanged).

---

# DOMAIN 2 — Common Landing Screen *(second deliverable)*

### RA-S16 · 🟢 DONE · Premium V* landing already exists with Parent + Admin entries and images
- **Location:** `composeApp/.../ui/v2/screens/auth/CommonLandingScreenV2.kt` (698 lines).
- **Verification (this is the "build a new premium landing page" deliverable — it is already met):**
  - **V* design system only** — uses `VTheme`, `VColors`, `VType`, `VCard`, `VBadge`, `VBrandLogo`, `VIcons`, `VMotion`, `pressScale`. No legacy Material-3 chrome.
  - **Legacy content carried forward** — teal hero with wordmark + tagline + trust pill; **Featured Institutions** photo rail; trust strip; **Next-Gen Intelligence** showcase (CMS `list_of_offerings`); **Portal access** rail (CMS `list_of_portals`); Terms/Privacy footnote. CMS-driven via `LandingViewModel → GET /api/v1/content/landing` with graceful hardcoded fallbacks.
  - **Both logins present** — `RoleCard("I'm a Parent", onClick = onParent)` (→ OTP auth) **and** `RoleCard("School / Administration", onClick = onAdmin)` (→ credential auth). The Portal-access rail also routes to `onAdmin`. (Teachers intentionally sign in through the Admin path — their credentials are minted inside a school.)
  - **Images already used** — `landing_school_1.webp`, `landing_school_2.webp`, `landing_school_3.webp` from `composeApp/src/commonMain/composeResources/drawable/`, used as `fallbackSchoolPhotos` behind the Featured Institutions rail (server `imageUrl` first, bundled photo fallback). Real photography → does not look AI-generated.
- **Conclusion:** No rebuild required. The deliverable is satisfied by existing code. Only the fabricated social-proof copy needs cleanup — see RA-S13.

### RA-S13 · 🟡 MEDIUM · ✅ FIXED · Fabricated social proof removed from the landing trust strip
- **Fix:** The fabricated `"240+ schools · 38k parents trust us"` strip and the `"Bank-grade encryption"` hero pill were both removed from `CommonLandingScreenV2.kt`. The hero now carries the legacy two-line value statement verbatim ("Education with Trust." / "Progress with Purpose.", CMS-overridable via `cms.topTagline`/`cms.subTagline`). The trust strip was replaced by the legacy `SocialProofSection` — the "TRUSTED BY 500+ INSTITUTIONS" eyebrow over the three legacy partner marquee labels (Academix / GlobalView / EduPulse), which are reference brand labels, not invented metrics. (Tracked together with RA-S20.)
- **Location:** `CommonLandingScreenV2.kt` lines ~250 and ~295–296.
- **Problem:** The trust strip hardcodes `"240+ schools" · "38k parents trust us"` and the hero shows a `"Bank-grade encryption"` pill — neither is sourced from data, and the numbers are invented.
- **Evidence:**
  ```kotlin
  withStyle(...) { append("240+ schools") }
  withStyle(...) { append(" · 38k parents trust us") }
  // and
  Text("Bank-grade encryption", …)
  ```
- **Impact:** Invented metrics are a trust/compliance risk (false advertising) on the very first screen.
- **Fix spec:** Either (a) source these counts from a real CMS/landing field (`cms?.trustStats`), showing the strip only when present; or (b) replace with non-numeric, defensible copy ("Trusted by schools across India", "Encrypted in transit & at rest" — only if true). Gate behind data; never invent.

---

# DOMAIN 3 — Parent Portal

### RA-S05 · 🟡 MEDIUM · ✅ FIXED · Multi-child `selectedChildId` is not propagated across parent ViewModels

**Fix (server `8350888` + shared `9d85d01`):** New `core/state/SelectedChildHolder` (Koin `single` exposing `StateFlow<String?>`, with `select` / `selectIfUnset` / `clear`) is the single source of truth for the parent's active child. `ParentHomeViewModel`, `ParentAcademicsViewModel`, and `ParentLeaveViewModel` now **observe** the holder (a child picked on one tab updates the others) and **publish** their pick via `selectChild`; on load they seed the holder with the default first child via `selectIfUnset` (without clobbering an explicit choice). `FeeViewModel` observes the holder and re-fetches fees scoped to the active child — `getFees(token, childId?)` was threaded through `ParentApi` / `ParentRepository` / `ParentRepositoryImpl`, and the server `GET /api/v1/parent/fees` gained an optional `?child_id=` filter (parent-scoped fetch, then in-memory child filter — Postgres-portable). `AuthRepositoryImpl.logout()` calls `selectedChildHolder.clear()` so a re-login as a different parent starts fresh (the single survives logout). `childId` defaults to `null` everywhere → backward-compatible (all-records view when nothing is selected).
- **Location:** `shared/.../feature/parent/presentation/{ParentHomeViewModel, ParentAcademicsViewModel, FeeViewModel, ParentMessageViewModel, ParentProfileViewModel}.kt`.
- **Problem:** Each parent ViewModel owns its **own** `selectedChildId` with no shared source of truth. `FeeViewModel.getFees(token)` takes no `childId` at all. Switching the active child on one tab does not change the others.
- **Impact:** A parent with two children who selects "Child B" on the Home tab still sees "Child A" data on Academics/Fees. Confusing and potentially shows the wrong child's marks/fees.
- **Fix spec:** Introduce a single shared `SelectedChildHolder` (a `single` in Koin exposing `StateFlow<String?>`); each parent ViewModel observes it instead of holding its own. Thread `childId` into `FeeViewModel.getFees(token, childId)` and the corresponding `/parent/fees?child_id=` query.

### RA-S06 · 🟡 MEDIUM · ✅ FIXED · `ParentPortalV2` hardcodes avatar name "Parent" and an always-red bell dot

**Fix (`d2a8ac7`):** (a) `TrackProgressViewModel` now exposes `accountName` (observed reactively from the RA-S03 `USER_NAME` pref); `ParentPortalV2`'s account avatar uses it (`accountName.ifBlank { "Parent" }`). (b) `ParentPortalV2` injects the shared `NotificationsViewModel`; the bell's `VStatusDot` renders only when `unreadCount > 0`. (c) The same always-on dot on `SchoolHomeScreenV2` was fixed identically — it injects `NotificationsViewModel` (the `GET /api/v1/notifications` feed is JWT-scoped and role-agnostic, so it serves admins too) and gates the dot on `unreadCount > 0`.

- **Location:** `composeApp/.../ui/v2/screens/parent/ParentPortalV2.kt` — line **225** (`VAvatar(name = "Parent", …)`) and line **221** (`VStatusDot(color = c.dangerInk, …)` rendered unconditionally).
- **Problem:** The header avatar always shows initials for the literal string "Parent" (because the real name is never persisted — see RA-S03), and the notification bell **always** shows a red unread dot regardless of whether there are unread notifications.
- **Impact:** Every parent sees a generic "P" avatar and a permanent "you have notifications" red dot that never clears — the dot becomes meaningless (cry-wolf) and the avatar feels impersonal.
- **Fix spec:** (a) Bind the avatar to the real display name (after RA-S03 lands). (b) Drive the bell dot from the real unread count (`notifications` / `unreadCount` from the notifications feed); render `VStatusDot` only when `unreadCount > 0`. The same always-on-dot pattern exists on the admin home (`SchoolHomeScreenV2.kt` line 143) — fix both.

*(Parent messaging reply-only is folded into DOMAIN 4 / RA-S07.)*

---

# DOMAIN 4 — Communication System

### RA-S07 · 🟠 HIGH · ✅ FIXED · Compose-new messaging — ADMIN + PARENT can now INITIATE conversations
- **Fix (ADMIN side):**
  1. **`MessagesViewModel.kt`** — `sendMessage` now passes `recipientUserId` into `SendMessageRequest(threadId, recipientUserId, body)` (was dropped before, so a null-thread send could never start a real 1:1). Added `ComposeState`/`MessageRecipient` UI models, injected `TeachersRepository`, and added `openCompose()` (loads the school's teachers as recipient candidates — `TeacherAccountDto.id` IS the recipient's `app_users` id), `closeCompose()`, and `composeNew(recipientUserId, body)` (→ `sendMessage(threadId = null, recipientUserId)`).
  2. **`Koin.kt`** — `MessagesViewModel` factory now 3-arg `(get(), get(), get())` to inject the already-registered `TeachersRepository` single.
  3. **`MessagesScreenV2.kt`** — added a "New message" `VButton` at the top of the thread list, a `ComposeNewContent` sheet (recipient picker over `VStateHost` + bottom composer) and a `RecipientRow` (avatar + name/role + "Selected" badge). Compose is the topmost layer (back closes it first).
- **Fix (PARENT side):**
  1. **`ParentMessagesRouting.kt`** — NEW `GET /api/v1/parent/messages/recipients`: resolves the parent's school from the child (never the body / a user id), lists every active **teacher + admin/school_admin** in that school, and flags teachers who teach one of the child's grades (via `TeacherPeriodsTable`) as the **class teacher** (sorted first). Uses the project's portable OR-reduce (no `inList`, Exposed-version-safe). The existing `POST /api/v1/parent/messages` already accepted `recipient_user_id`, so no new send path was needed. Registered automatically (lives inside `parentMessagesRouting()`).
  2. **shared client** — `ParentRecipientDto`/`ParentRecipientsResponse` models; `ParentRepository.getMessageRecipients()` + impl + `ParentApi.getMessageRecipients()` (`GET …/recipients`).
  3. **`ParentMessageViewModel.kt`** — added compose-new state (`composeOpen`/`composeLoadingRecipients`/`composeRecipients`/`composeError` + `composeEmpty`), `openCompose()`/`closeCompose()`/`composeNew(recipientUserId, body)` (on success refreshes the inbox and opens the new thread).
  4. **`ParentMessagesScreenV2.kt`** — added a "New message" `VButton` on the inbox, a `ParentComposeNewContent` sheet (recipient picker + composer) and `ParentRecipientRow`. Back peels layers: compose-new → open conversation → exit.
- **Reply paths on both sides are untouched — this is purely additive.**
- **Location (UI):** `composeApp/.../ui/v2/screens/parent/ParentMessagesScreenV2.kt` (reply-only); `composeApp/.../ui/v2/screens/school/MessagesScreenV2.kt` (line 95 wires `onSend = viewModel::sendReply` only). **(API):** `server/.../feature/school/MessagesRouting.kt` `POST /messages` accepts `recipient_user_id` (line 76, 252); the admin VM (`shared/.../feature/admin/presentation/MessagesViewModel.kt`) exposes both `sendMessage(recipientUserId)` and `sendReply`.
- **Problem:** The two-row conversation engine (`MessagingCore.sendInConversation`) fully supports **starting a new 1:1 thread** by passing `recipient_user_id`. But:
  - The **admin** screen only ever calls `sendReply` — there is **no "New message" button, no recipient picker** in `MessagesScreenV2`. `sendMessage(recipientUserId)` is dead UI-wise.
  - The **parent** screen is likewise reply-only (`ParentMessageViewModel.reply()` requires an `openThreadId`; there is no compose-new path).
  - For contrast, the **teacher** side *does* expose compose + class-broadcast (`TeacherMessagesRouting` `POST /messages` with `recipient_user_id` and `POST /messages/class`), and the teacher UI wires it.
- **Impact:** A parent can only reply if a teacher/admin messaged them first; an admin can only reply to threads parents started. Neither can **initiate** contact, which is a core CRM expectation. The capability exists in the backend but is unreachable from two of three portals.
- **Fix spec:**
  1. **Admin:** add a "New message" affordance that opens a recipient picker (parents/teachers in the school via an existing roster endpoint), then call `viewModel.sendMessage(recipientUserId = …, body = …)`. The API + VM already support it.
  2. **Parent:** add "Message my child's teacher / school" — resolve the recipient from the linked child's class teacher / admin desk, then POST with `recipient_user_id`. (A dedicated parent compose endpoint may be needed if the parent route doesn't yet accept `recipient_user_id`; verify `ParentMessagesRouting` and add it symmetrically if missing.)
  3. Keep the reply path intact; this is additive.

### RA-S08 · 🟠 HIGH · ✅ FIXED · Admin (and parent) direct message now notifies the recipient
- **Fix (root, no drift):**
  1. **`MessagingCore.kt`** — `SendResult` gained a resolved `recipientId` (the peer of the send, read off the existing thread row on appends or the explicit `recipientId` on new conversations; null only for self/system threads). Added a SHARED `internal suspend fun notifyMessageRecipient(recipientId, schoolId, actorId, actorName, threadId, body)` (best-effort, includes the message body, deep-links to `parent/messages`).
  2. **Admin** (`MessagesRouting.kt` POST) now calls `notifyMessageRecipient(result.recipientId, …, body = req.body)` after a successful send — works for both new-thread and append sends.
  3. **Teacher** (`TeacherMessagesRouting.kt`) switched from its private (body-less) helper to the shared one (its duplicate was deleted) — fixes drift AND its previously-empty notification body.
  4. **Parent** (`ParentMessagesRouting.kt`) also wired for parity, so a parent's reply notifies the teacher/admin.
- **Original finding (for reference):** when the admin sent via `sendInConversation`, the handler returned success but never called `Notify`, so the recipient got a thread but no notification (broke parity with the teacher path).
- **Location:** `server/.../feature/school/MessagesRouting.kt`, `MessagingCore.kt`, `feature/teacher/TeacherMessagesRouting.kt`, `feature/user/ParentMessagesRouting.kt`.
- **Problem:** When the admin sends a message via `sendInConversation`, the handler returns success but **never calls `Notify`**. Compare the teacher handler (`TeacherMessagesRouting.kt` lines 271, 338–358) which calls `notifyMessageRecipient(...)`/`Notify.toUsers` on every send.
- **Evidence:** `MessagesRouting.kt` POST block contains `sendInConversation(...)` then `call.created(...)` — no `Notify.*` call anywhere in the file (grep for `Notify` in `MessagesRouting.kt` → none).
- **Impact:** A parent gets a message from the school in their thread list but **no notification** — they only discover it if they happen to open Messages. Engagement and timeliness suffer; it also breaks parity with the teacher path.
- **Fix spec:** After a successful `sendInConversation`, mirror the teacher path: if `recipientId != null && recipientId != ctx.userId`, call `Notify.toUser(userId = recipientId, category = "message", title = "Message from <school>", body = req.body.take(120), schoolId = ctx.schoolId, actorId = ctx.userId, deepLink = "parent/messages", refType = "message", refId = threadId)`. Extract a shared `notifyMessageRecipient` helper to avoid drift between admin/teacher.

**OK / verified:** No delete-thread or delete-message routes exist on any side (good — messaging is append-only/auditable). The two-row conversation model (`MessageThreadsTable` per-owner + shared `conversation_id`, `MessagesTable`) is consistent across parent/teacher/admin via `MessagingCore`.

---

# DOMAIN 5 — School Admin Portal

### RA-S10 · 🔴 CRITICAL · ✅ FIXED `23ce113` · `PewsPreview` rewritten as a label-free schematic (no fabricated students)
- **Fix:** `PewsPreview.kt` was rewritten as a purely schematic teaser — `RiskBandTile` (colour band + band label, **no counts**), `PlaceholderRow` (avatar dot + grey bars), and generic intervention copy. All fabricated student names/metrics (`atRisk`, "180 students scored", "Low 142 / Watch 27 / High 11") were removed. `dashedBorder` modifier preserved. Sole render site `SchoolHomeScreenV2.kt:212` unchanged (still inside the coming-soon teaser).
- **Location:** `composeApp/.../ui/v2/screens/school/PewsPreview.kt`; rendered live by `SchoolHomeScreenV2.kt` line 212 (`preview = { PewsPreview() }`).
- **Problem:** The admin home's "Early-warning radar" card embeds `PewsPreview()`, which displays **hardcoded fake students and metrics**:
  ```kotlin
  val atRisk = listOf(
      Triple("Aman Verma", "9-B", 78) to "Attendance ↓ 22%, Maths −18 pts",
      Triple("Ishita Roy", "10-A", 64) to "Late submissions, mood dip flagged",
      Triple("Karan Singh", "9-A", 52) to "Fee overdue, syllabus gap",
  )
  // plus "180 students scored", risk tiles Low 142 / Watch 27 / High 11,
  // and a fabricated "SUGGESTED INTERVENTION" sentence.
  ```
- **Impact:** An admin sees **named "at-risk" students that do not exist** and invented risk counts on their home screen. This is a data-integrity / honesty violation (LAW 6: never fabricate data) and is actively misleading — an admin could act on fictitious "Aman Verma". The *real* at-risk cohort IS available (`onOpenPews` switches to the People tab, which renders `state.atRiskStudents` from `GET /api/v1/student-cohort` — that part is correct), making the fake preview both unnecessary and dangerous.
- **Fix spec:** Replace the hardcoded preview body with either (a) a **schematic, label-free** teaser (no names, no numbers — purely illustrative chrome that clearly reads as "feature preview"), or (b) bind it to the real `student-cohort` aggregate (top 3 at-risk + real band counts) behind a `VStateHost` so it shows skeleton/empty honestly. Do **not** ship invented student names anywhere in production.

### RA-S09 · 🟠 HIGH · ✅ FIXED · Admin results-publish now notifies class parents
- **Fix:** After the bulk upsert in `ResultsRouting.kt` `POST` (publish), resolve affected parents via `NotifyRecipients.parentsOfClass(schoolId, req.className)` and fan out `Notify.toUsers(category = "marks", title = "Results published", body = "Marks for \"<test>\" (<subject>) have been published.", deepLink = "parent/academics/marks", refType = "assessment", refId = req.test)`. Scoped to `ctx.schoolId` for tenant isolation; wrapped in `runCatching` (best-effort, never fails the publish). Mirrors the teacher marks-submit notification shape exactly, so the notification model is now symmetric across teacher + admin.
- **Location:** `server/.../feature/school/ResultsRouting.kt` (publish handler).
- **Original finding (for reference):** the publish route upserted rows and returned success but never called `Notify` — parents were notified for teacher-entered marks but not admin-published results.
- **Problem:** When an admin publishes results, the route upserts rows and returns success but **never calls `Notify`** (grep for `Notify` in `ResultsRouting.kt` → none). Contrast the teacher marks-submit path (`TeacherRoutingTasks.kt` lines 444–460) which notifies every class parent ("Results published") via `Notify.toUsers`.
- **Impact:** Parents are notified when a *teacher* enters marks but **not** when an *admin* publishes results — inconsistent, and parents miss admin-published results entirely.
- **Fix spec:** After the upsert, resolve affected parents (`NotifyRecipients.parentsOfClass(schoolId, className)` or per-student `parentsOfStudent`) and call `Notify.toUsers(category = "marks", title = "Results published", body = "…", deepLink = "parent/academics/marks", refType = "assessment", refId = …)`, scoped to `ctx.schoolId`. Reuse the teacher path's notification shape for consistency.

### RA-S12 · 🟡 MEDIUM · ✅ FIXED · Comms-tab badge hardcoded to `2`

**Fix (`71291b1`):** `SchoolPortalV2` now injects `MessagesViewModel` (which loads `GET /api/v1/school/messages/threads` on init) and sets `VNavItem("comms", …, badge = commsBadge)` where `commsBadge = threads.count { it.unreadCount > 0 }`. `VBottomNav` already only renders the badge when `> 0`, so an all-read inbox shows no badge — the permanent fake `2` is gone.
- **Location:** `composeApp/.../ui/v2/screens/school/SchoolPortalV2.kt` line 169 — `VNavItem("comms", "Comms", VIcons.Megaphone, badge = 2)`.
- **Problem:** The bottom-nav Comms badge is a literal `2`, not derived from any unread/pending count.
- **Impact:** Always shows "2" regardless of actual unread messages/announcements — a permanent fake badge.
- **Fix spec:** Drive `badge` from a real count (unread message threads + pending items) exposed by the comms/messages VM; omit the badge when zero.

### Admin portal — verified-correct surfaces (🟢, documented to avoid re-investigation)
- `SchoolHomeScreenV2` onboarding hero is **real** (from `SchoolDashboardViewModel` → `GET /api/v1/user/details`); the "Live campus metrics" and PEWS cards open the **real** `AnalyticsDashboardScreenV2` and People/cohort tab (RA-24 lineage).
- `AnalyticsDashboardScreenV2` is fully wired to `AnalyticsDashboardViewModel` → `GET /api/v1/school/analytics/overview` (trend chart, cards, insights all from state; honest empty state).
- `SchoolCommsScreenV2` announcements are **real** (`SchoolAnnouncementsViewModel` → `GET/POST /api/v1/announcements`) with audience targeting (ALL_SCHOOL/CLASS/SUBJECT/STUDENT, RA-49) and a working compose dialog. Messages/PTM open real backend screens; only the "Notifications delivery log" is an honest `VComingSoon`.
- `AnnouncementRouting` **does** notify (audience-scoped `Notify.toUsers` over `NotifyRecipients.parentsForAudience` + teachers for ALL_SCHOOL) — earlier suspicion of a missing announcement notify was **wrong**; verified present (lines 35–36, 229–250).
- Admin overlays (Leave/Link requests, Admissions CRM, Results, PTM, DailyAttendance, Class/Teacher performance, Student roster/profile, Edit institutional profile) are all wired to real screens in `SchoolPortalV2`.

---

# DOMAIN 6 — Teacher Portal

### RA-S15 · 🟢 VERIFIED GOOD · Teacher write paths reach Supabase, are scoped, and notify parents
- **Location:** `server/.../feature/teacher/{TeacherRouting.kt, TeacherRoutingTasks.kt, TeacherLeaveRouting.kt, TeacherMessagesRouting.kt}`; client `shared/.../feature/teacher/data/remote/TeacherApi.kt`.
- **Verification:**
  - **Attendance** (`POST /attendance`) — upserts `attendance_records` (type=student) per entry, validated against `present|absent|late`, scoped via `requireOwnedAssignment`; **notifies each affected parent** on absent/late (RA-41).
  - **Marks** (`POST /marks`) — upserts `assessment_marks`, clamps to `max_marks`, then **publishes the assessment** (`isPublished = true`) so parents can read it, and **notifies class parents** ("Results published"). The marks plane is reachable because `/assessments` (list+create) exists (RA-40) — confirmed `TeacherUpdateSelector.kt` uses real `GET /teacher/classes` + `GET /teacher/assessments`.
  - **Syllabus** (`PATCH /syllabus`) — toggles `syllabus_units.is_covered` with `covered_on/covered_by`, authorized to the teacher's own class+subject.
  - **Homework** (`POST /homework`) — inserts `homework` and **notifies class parents** ("New homework").
  - **Leave approval** (`PATCH /teacher/leave-requests/{id}`) — scoped to the teacher's own classes (direct or by class/section), and **notifies the applicant parent** of approve/reject (RA-44).
  - **Messaging** (`TeacherMessagesRouting`) — supports reply, **compose-new** (`recipient_user_id`), and **class broadcast** (`POST /messages/class` fan-out to every class parent), each with `Notify` (RA-51).
  - **Multi-tenancy** — every handler resolves a trusted `TeacherContext` (`requireTeacherContext`) and uses `ctx.schoolId`; class-scoped calls go through `requireOwnedAssignment`. No `school_id` from the request body.
- **Conclusion:** The teacher vertical is the strongest part of the app. **No write-path defects found.** Client `TeacherApi` wires all reads/writes to these routes. Leave RA-S15 as a green reference.

*(No teacher-side TODO/FIXME or fabricated data was found in the teacher screens during the DOMAIN 9 sweep.)*

---

# DOMAIN 7 — Cross-User Flows & Notifications

The notification spine (`server/.../feature/notifications/Notify.kt` + `NotifyRecipients.kt`, `NotificationsTable`) is **DB-only** (no push/SMS yet — honest `VComingSoon` on the admin "delivery log"). Cross-role triggers, audited end-to-end:

| Trigger | Notifies? | Verdict |
|---------|-----------|---------|
| Teacher marks attendance absent/late | ✅ parents of student | OK |
| Teacher submits marks | ✅ class parents (+ auto-publish) | OK |
| Teacher creates homework | ✅ class parents | OK |
| Teacher decides leave | ✅ applicant parent | OK |
| Teacher sends message / class broadcast | ✅ recipient(s) | OK |
| Admin publishes announcement | ✅ audience-scoped parents (+teachers if ALL_SCHOOL) | OK |
| **Admin publishes results** (`ResultsRouting`) | ✅ class parents (RA-S09 fixed) | **RA-S09** |
| **Admin sends direct message** (`MessagesRouting`) | ❌ **none** | **RA-S08** |
| Parent ↔ school/teacher link request decisions | ⚠️ verify | see note |

- **Note (link-request notifications):** Verify `LinkRequestsRouting` / `ParentLinkRouting` notify the parent when an admin approves/rejects a child-link request (mirrors the teacher leave pattern). If absent, file as a follow-up HIGH (same shape as RA-S08/09). This was not exhaustively traced; flag for the implementer.
- The two confirmed gaps (RA-S08, RA-S09) make the notification coverage **asymmetric**: teacher actions are fully wired, admin actions are partially wired. Closing both makes the notification model uniform.

---

# DOMAIN 8 — Multi-Tenancy & Security

### RA-S14 · 🟢 VERIFIED SECURE · `school_id` and `role` are always derived server-side from the JWT-resolved DB row
- **Location:** `server/.../core/{JwtConfig.kt, SchoolAccess.kt, TeacherAccess.kt}`; consumed by 122 call-sites (`requireSchoolContext / requireTeacherContext / requireSchoolAdmin / requireParentContext`).
- **Verification:**
  - The JWT carries **no `school_id` and no role claim** for authorization; `requireSchoolContext()` (and the teacher/parent equivalents) read `school_id` and `role` from `app_users` keyed by the JWT `sub`. So a forged/edited claim cannot widen scope.
  - A repo-wide grep for `school_id` sourced from the **request body/query** returns only **two** hits, both verified safe:
    1. `AnnouncementRouting.kt` — a `?school_id=` / body override exists but `effectiveSchoolId(ctxSchoolId, ctxRole, override)` returns `ctxSchoolId` for everyone except the platform `"admin"` role (lines 127–130). A `school_admin` override is **ignored** → cannot read/mutate another school.
    2. `ParentLinkRouting.kt:160` — `req.schoolId` is the school a parent is *requesting to link to* (legitimate input for cross-school discovery/linking, gated by the link-approval workflow), not an authorization scope.
  - All school-scoped queries filter on `ctx.schoolId`; teacher class access additionally goes through `requireOwnedAssignment`.
- **Conclusion:** No tenancy bypass found. This is the correct pattern — keep it. Do **not** introduce client-trusted `school_id` (reinforces the RA-S03 note).
- **Standing rule for new routes:** every new school-scoped endpoint MUST resolve scope via `requireSchoolContext()`/`requireTeacherContext()` and filter on `ctx.schoolId`; never accept `school_id` from the body except behind the platform-admin gate.

---

# DOMAIN 9 — Hardcoding Sweep

Grep-driven sweep across `composeApp/commonMain`, `shared/commonMain`, `server/main`.

| Pattern | Result |
|---------|--------|
| `MockV2` in live paths | **None.** All matches are comments documenting its removal (Discovery, Calendar, Notifications, ParentActivity, ParentAcademics, ParentLinkChild). |
| Hardcoded `"NN%"` in UI screens | **None** — percentages render from data. |
| `TODO/FIXME/HACK/XXX` | 5 occurrences — minor; none security-relevant. Track but low priority. |
| `VComingSoon` | Used honestly (real "not built yet" surfaces): `SchoolCommsScreenV2` (delivery log), `DiscoveryScreenV2`, `SchoolRecordsScreenV2`, `TeacherClassesScreenV2`, plus the preview-teaser hosts. Acceptable per LAW 6. |
| **Fabricated data in `*Preview` composables shown in production** | **2 real problems:** `PewsPreview` (RA-S10, admin home) and `AiReportCardPreview` (RA-S11, parent academics). `SriPreview` is gated inside a `VComingSoon` teaser (acceptable). |
| Hardcoded UI strings | `ParentPortalV2` "Parent" (RA-S06); landing "240+ schools / 38k parents / Bank-grade encryption" (RA-S13); `SchoolPortalV2` Comms badge `= 2` (RA-S12); always-red bell dots (RA-S06, admin home line 143). |

### RA-S11 · 🔴 CRITICAL · ✅ FIXED `7ee5271` · `AiReportCardPreview` rewritten as a label-free schematic (no fabricated grades)
- **Fix:** `AiReportCardPreview.kt` was rewritten as a purely schematic teaser — an `InsightChrome` framing, `GradePlaceholderCard`s and abstract narrative bars replace the fabricated grades/AI narrative for "Riya". Sole render site `ParentAcademicsScreenV2.kt:189` (Report tab `VComingSoon` preview) unchanged.
- **Location:** `composeApp/.../ui/v2/screens/parent/AiReportCardPreview.kt`; rendered by `ParentAcademicsScreenV2.kt` line 189 (`preview = { AiReportCardPreview() }`).
- **Problem:** Same class of issue as RA-S10 — a parent sees **fabricated grades and AI "insights"** as a feature preview inside their real academics screen.
- **Impact:** A parent could mistake invented marks for their child's actual results. Honesty/data-integrity violation in a parent-facing portal.
- **Fix spec:** Convert to a label-free schematic teaser, or bind to the child's real published-marks aggregate behind `VStateHost`. No invented grades in production.

---

# ADDENDUM — User-reported issues (RA-S17 … RA-S20)

Audited 2026-06-09 against the live code. Static analysis only.

### RA-S17 · 🟠 HIGH · ✅ FIXED · Admin People tab: no sub-tabs, no non-teaching staff, no search/filter, and delete is a direct row button instead of living inside the profile *(newly reported)*

**Re-verification + resolution (2026-06-09, continuation pass):** prior commits `15ede8c…901e145` were re-checked against source — RA-S01/S02/S04/S07/S08/S09/S10/S11/S13/S18/S19/S20 + LEGAL-01 confirmed genuinely fixed. RA-S17 was the only HIGH still entirely open; RA-S03/S05/S06/S12 (MEDIUM) confirmed still open. RA-S17 is now **fully shipped end-to-end** across all four gaps:
  - **Server (`93bfbde`):** `NonTeachingStaffTable` (school-scoped, soft-delete) added to `Tables.kt` + `DatabaseFactory.allTables`; `NonTeachingStaffRouting` — `GET/POST/GET{id}/PATCH{id}/DELETE{id}` at `/api/v1/school/staff`, admin-gated writes via `requireSchoolAdmin`, every query scoped to `ctx.schoolId` (RA-S14 invariant held — `school_id` never from body); registered in `Application.kt`. `GET /school/students` gained `?q=&class=` search/filter (in-memory after scoped fetch — Postgres-portable, no `inList`/`ILIKE`).
  - **Shared (`eb4fece` + `b7ebd21`):** `StaffModels` (DTOs mirror server), `StaffRepository`/`StaffRepositoryImpl`/`StaffApi`, `StaffViewModel` (three-state roster + search + add/update/remove with `removingIds`), all registered in Koin. `TeacherProfileViewModel` gained `TeachersRepository` + `remove()`; `StudentProfileViewModel` gained `remove()`; both expose `isRemoving/removed/removeError`.
  - **UI (`c8a918b` + `936021a`):** `TeacherProfileScreenV2`/`StudentProfileScreenV2` gained a "Remove from school" `VButton(Destructive)` + `VConfirmDialog` + `onRemoved` callback (delete now lives **inside the profile**). New `StaffProfileScreenV2` mirrors the same delete-in-profile pattern (resolves the member from the roster VM, CONTACT section, three states via `VStateHost`). `SchoolPeopleScreenV2` rewritten into a `VTopTabs` **3-sub-tab** layout (Teachers / Students / Non-teaching staff); each sub-tab has a search `VInput` + tappable `PersonRow` (no row-delete) + an `AddStaffDialog`; cohort analytics moved under Students. `SchoolPortalV2` wired: new `Staff` overlay + `selectedStaffId`, `StaffProfileScreenV2` overlay handler, People call site updated to the new signature (`onOpenStudent`/`onOpenTeacher`/`onOpenStaff`; dropped `onOpenStudentRoster`), `onRemoved` passed to all three profiles to pop+refresh on delete.
  - **All four gaps closed:** (1) sub-tabs ✅; (2) non-teaching staff exists end-to-end ✅; (3) search/filter per sub-tab ✅; (4) delete moved into the profile behind a confirm dialog, direct row "Remove" button removed ✅.


- **Location (UI):** `composeApp/.../ui/v2/screens/school/SchoolPeopleScreenV2.kt` (504 lines). `TeacherRosterRow` (lines 348–378) renders a direct **"Remove"** `VButton` (lines 368–375) → `pendingRemoval` → `VConfirmDialog`. The tab is a flat scroll (People title → child-link-requests card → student-roster card → `TeacherRosterSection` → cohort analytics). Profiles open via `onOpenTeacher(t.id)` / student roster.
- **Location (profiles, where delete SHOULD live):** `StudentProfileScreenV2.kt` and `TeacherProfileScreenV2.kt` — **no delete affordance exists** (grep returned none).
- **Location (backend):**
  - Students: `server/.../feature/school/SchoolStudentsRouting.kt` — `GET /api/v1/school/students` has **no search/filter query params** (orders by class/roll only); `delete("{id}")` (line 214) does a school-scoped, admin-gated **soft delete**. ✅ delete exists.
  - Teachers: `server/.../feature/school/TeacherProvisioningRouting.kt` — `route("/api/v1/school/teachers")` (line 101); `delete("/{id}")` (line 202). ✅ delete exists.
  - **Non-teaching staff: NO table, NO route, NO model, NO UI anywhere.** This is a brand-new feature, not a wiring fix.
- **Problem:** Four distinct gaps vs the user's spec — (1) no sub-tabs to segment Teachers / Students / **Non-teaching staff**; (2) non-teaching staff does not exist as a concept in the codebase; (3) no search box / filter chips on the tab (search exists only inside add-dialogs); (4) deletion is a direct, easily-mis-tapped row button ("that's shit") rather than a deliberate action inside the person's profile behind a confirmation popup.
- **Evidence:** `TeacherRosterRow` lines 368–375 (direct Remove button); profiles grep → zero delete affordance; `SchoolStudentsRouting` GET has no `q`/`filter` params; no `*StaffTable` / `*StaffRouting` files exist.
- **Impact:** People management is shallow and error-prone; a whole staff category is unmanageable; admins cannot find a person in a large school; destructive delete is one tap from the list.
- **Fix spec:**
  1. **UI:** Replace the flat scroll with a `VTopTabs`-driven 3-sub-tab layout (`Teachers` / `Students` / `Non-teaching staff`). Add a `VInput` search field + filter chips (e.g. class/section for students, role/department for staff) at the top of each sub-tab. Each row is **tap-to-open-profile only** — **delete the direct "Remove" `VButton`** (lines 368–375) and the `pendingRemoval`/`VConfirmDialog` from this screen.
  2. **Profiles:** Add a "Remove from school" action inside `TeacherProfileScreenV2` and `StudentProfileScreenV2` (and a new staff profile), each guarded by `VConfirmDialog`, wired to the existing `delete` routes (teachers/students) — soft-delete, school-scoped.
  3. **Backend:** Add `q`/filter query params to `GET /school/students` (and the teachers list). **Build the non-teaching-staff vertical end-to-end**: `NonTeachingStaffTable` (school-scoped, soft-deletable, role/department fields) + CRUD route `/api/v1/school/staff` (admin-gated, `effectiveSchoolId` per RA-S14) + shared model/API/repo + ViewModel + profile screen.
  4. All UI via V* primitives + `VTheme` tokens only.

### RA-S18 · 🟠 HIGH · ✅ FIXED · Teacher Submit (attendance **+ marks**) now reflects the real network result *(newly reported; extended to MARKS per user directive)*
- **Fix (root, not symptom):**
  1. **`VButton.kt`** gained an external `success: Boolean = false` param. Result-driven callers now pass `stateful = false` with real `loading` (spinner in flight) and real `success` (check + successLabel once the VM confirms). External `success` wins over `loading`; the legacy `stateful` fake-timer path is retained only for backward-compat and is now unused.
  2. **Teacher attendance** (`TeacherAttendanceScreenV2.kt` + `TeacherAttendanceViewModel.kt`): Submit drops `stateful`; uses `loading = isSubmitting`, `success = submitSuccess`, disabled while submitting/after a confirmed save (re-enables on roster edit via `setStatus`/`markAll` clearing `submitSuccess`). Split state into `error` (load → VStateHost) vs `submitError` (inline) so a failed save never wipes the marked roster.
  3. **Teacher marks** (`TeacherMarksScreenV2.kt` + `TeacherMarksViewModel.kt`): identical fix applied per user directive — "Save marks" is result-driven, re-enables when a mark is edited (`setMark` clears `submitSuccess`), and surfaces a `submitError` inline.
- **Original finding (for reference):** attendance "Submit" never reflected success — `VButton(stateful=true)` ran a fake time-based animation; the DB write itself was correct.
- **Location:** `composeApp/.../ui/v2/screens/teacher/TeacherAttendanceScreenV2.kt` Submit button (lines 145–153):
  ```kotlin
  VButton(
      text = "Submit",
      onClick = onSubmit,            // viewModel::submit
      size = VButtonSize.Lg,
      tone = VButtonTone.Lavender,
      stateful = true,               // ← ROOT CAUSE
      loading = state.isSubmitting,  // ← IGNORED because stateful=true
      successLabel = "Submitted",
  )
  ```
- **Root cause:** `composeApp/.../ui/v2/components/VButton.kt` — when `stateful = true`, `activePhase` comes from the button's **own** `phase` var (lines 137–150), driven by a time-based `LaunchedEffect(phase)` (Loading → `delay(650)` → Success → `delay(1400)` → Idle). The click handler (lines 232–238) sets `phase = Loading` then calls `onClick()`, with **no coupling between `phase` and the submit result**. The external `loading`/`state.isSubmitting` is ignored and `state.submitSuccess` is **never wired in**. So the button animates a fabricated "Submitted" then reverts to "Submit" regardless of the network outcome.
- **Is the DB updated? YES.** `shared/.../feature/teacher/presentation/TeacherAttendanceViewModel.kt` `submit()` correctly sets `submitSuccess = true` on `NetworkResult.Success` from `repository.submitAttendance(...)`. The server `TeacherRoutingTasks.kt` POST `/attendance` (lines 236–307) upserts `attendance_records` (scoped via `requireOwnedAssignment`), validates `present|absent|late`, and notifies parents of absent/late. This is the same verified-good path as **RA-S15**. **The persistence works; only the button feedback is wrong.**
- **Evidence:** `stateful = true` on the Submit `VButton`; `VButton.kt` lines 137–150 / 232–238 show the time-based, result-independent phase machine; the screen never reads `state.submitSuccess`.
- **Impact:** Teachers cannot tell whether attendance saved; the persistent "Submit" invites duplicate submissions and erodes trust. (Harmless to data because the upsert is idempotent per day, but the UX bug is real.)
- **Fix spec:** Drive the button from real state — set `stateful = false` and use `loading = state.isSubmitting`; on `state.submitSuccess` either disable the button + show a success toast/`VBadge`, or render a result-driven success state (e.g. a `confirmed` flag the VM exposes). Reset `submitSuccess` when the roster changes so the next submission re-enables. Also surface `state.error` (currently unsurfaced) via `VStateHost`/snackbar.

### RA-S19 · 🟠 HIGH · ✅ FIXED · Parent attendance is now a month-calendar; marks/syllabus joins hardened against the section fallback *(newly reported)*
- **Fix (UI — attendance calendar):** New `composeApp/.../ui/v2/screens/parent/ParentAttendanceCalendar.kt` renders a true month grid (V* primitives + `VTheme` only, no kotlinx-datetime — manual ISO date math + Sakamoto day-of-week, mirroring `AcademicCalendarScreenV2`). Each day cell is colour-coded (present=`success`, late=`teal`, absent=`danger`), with a month pager (newest month first, derived from the data), weekday header, and a legend. `AttendanceTab` now calls `ParentAttendanceCalendar(records = data.records)` instead of the flat `data.records.take(30)` list; the summary `VCard` + `VStateHost` loading/error/empty are retained. No backend change needed — same `records[]` payload.
- **Fix (backend — marks/syllabus robustness):** `ResolvedChild` gained `sectionResolved: Boolean` (true only when `section` came from a linked `students` row). The marks query (`/marks`) and syllabus query (`/syllabus`) now apply the `section` filter **only when `sectionResolved` is true** — when section is the `"A"` fallback (child linked but students row missing/mismatched), the filter relaxes to class-level so published marks/syllabus are no longer silently hidden. The explicit "No published marks yet" empty state already exists in the UI.
- **Original finding (for reference):** attendance rendered as a flat 30-row list (not a calendar); the marks query filtered on a `section` that falls back to `"A"`, silently emptying the tab for linked-but-mismatched children.
- **Location (UI):** `composeApp/.../ui/v2/screens/parent/ParentAcademicsScreenV2.kt` — `AttendanceTab` (lines 196–242) renders a summary `VCard` + a flat `data.records.take(30)` list of `date → VBadge(status)` rows. `MarksTab` (lines 244–272) lists published results.
- **Location (VM):** `shared/.../feature/parent/presentation/ParentAcademicsViewModel.kt` — correctly lazy-loads attendance/marks/syllabus per tab via `repository.getChildAttendance/Marks/Syllabus`.
- **Location (backend):** `server/.../feature/parent/ParentAcademicsRouting.kt` — `GET /api/v1/parent/child/{id}/{attendance,marks,syllabus}`, ownership-gated by `requireOwnedChild()`, **registered** in `Application.kt` (lines 230–234).
- **Data join — VERIFIED CONSISTENT:** Teacher writes `attendance_records.person_id = student_code` (`TeacherRoutingTasks.kt` line 222/276); parent reads `person_id eq child.studentCode` (`ParentAcademicsRouting.kt` line 166). The path is wired and correct — **this is not a missing-endpoint problem.**
- **Problem (two real defects):**
  1. **Attendance is NOT calendar-based.** The user explicitly demands a **calendar** view; the screen shows a scrolling list of the last 30 date rows. No month grid, no day cells, no per-day color coding.
  2. **Marks visibility is fragile.** The parent marks query (`ParentAcademicsRouting.kt` lines 200–224) filters on `className == child.grade` **and** `section == child.section` **and** `is_published == true`. But `requireOwnedChild()` (lines 134–140) **falls back `section` to `"A"`** when the child has no linked `students` row, and `grade` falls back to the parent-typed `currentGrade`. A section/class mismatch — or teachers simply never publishing — yields a silently empty Marks tab that the parent experiences as "can't see marks". RA-S11 (fabricated `AiReportCardPreview` on the same screen) compounds the confusion.
- **Evidence:** `AttendanceTab` lines 225–239 (flat list, no calendar); `requireOwnedChild` lines 138–140 (`grade`/`section` fallbacks); marks filter lines 202–207 (`section`/`isPublished` gates).
- **Impact:** The single most important parent value (attendance + marks) is technically present but presented in a way that fails the user's expectation (calendar) and can silently show empty for linked-but-mismatched children.
- **Fix spec:**
  1. **Attendance → calendar.** Build a month-grid calendar component (V* primitives + `VTheme`) that maps `records[]` (date → status) onto day cells with status colors (present/late/absent), month paging, and the summary header retained. Keep `VStateHost` loading/error/empty. Backend already returns per-day records; if month paging is desired, add an optional `?month=YYYY-MM` query param to the attendance endpoint.
  2. **Marks robustness.** Resolve `class`+`section` from the **linked student row first** and only fall back to `currentGrade` when truly unlinked; when a child is linked but no published marks exist, show an explicit "No published marks yet — ask the class teacher to publish results" empty state (already partly present) and log the class/section used so mismatches are diagnosable. Consider relaxing the `section` filter to class-level when section is the unreliable `"A"` fallback.
  3. Remove/teaser-ize the fabricated `AiReportCardPreview` (tracked as RA-S11).

### RA-S20 · 🟡 MEDIUM · ✅ FIXED · Common landing rebuilt from the legacy reference content in the V* design system *(supersedes RA-S16's "DONE")*
- **Fix (content rebuild — per user directive):** `CommonLandingScreenV2.kt` was rebuilt to carry the **legacy reference `CommonLandingScreen` content verbatim**, implemented entirely in the current V* design system (V* primitives, `VColors` tokens, `VTheme` typography, `VMotion`) — zero legacy Material-3 components, zero React:
  - **Hero** — two-line value statement "Education with Trust." / "Progress with Purpose." (CMS-overridable), the bundled offerings-grid hero image, `VBrandLogo` + halo motion. The fabricated "Bank-grade encryption" pill was removed.
  - **Featured Institutions** — real-photo rail backed by the three bundled campus images (`landing_school_1/2/3.webp`) so it does not read as AI-generated, server `imageUrl` first.
  - **Social proof** — legacy "TRUSTED BY 500+ INSTITUTIONS" eyebrow + the three legacy partner marquee items (Academix / GlobalView / EduPulse). Replaces the fabricated "240+ schools · 38k parents" strip (RA-S13).
  - **Entry points** — the two legacy `EntryPointCard`s with their exact labels/titles/descriptions/feature lists/CTAs: "FOR PARENTS" → "Find the perfect school for your child's unique journey" → **Start Your Search** (→ onParent / OTP auth); "FOR SCHOOLS" → "Scale excellence with intelligence." → **Onboard Your School** (→ onAdmin / credential auth). CMS-overridable via `parentInfo`/`schoolInfo`.
  - **Next-Gen Intelligence** showcase — "Proprietary systems powering the ecosystem." (CMS `list_of_offerings`).
  - **Portal access** — legacy "Access Your Portal" / "Already part of the VidyaPrayag ecosystem?" / "View All Portals" header over the CMS `list_of_portals` rail.
  - **Footer** — legacy footer (wordmark, © line, PORTALS + SUPPORT link groups, Terms/Privacy).
  - Parent CTA → `ParentAuthScreen`, Admin CTA → `AdminAuthScreen` contract preserved.
- **Original finding (for reference):** the live landing read plain/generic and still showed the fabricated "Bank-grade encryption" pill + "240+ schools · 38k parents" strip.
- **Location:** `composeApp/.../ui/v2/screens/auth/CommonLandingScreenV2.kt` (698 lines).
- **Problem:** The attached screenshots confirm the surface renders with the correct structure (teal hero + `VBrandLogo` + "VidyaSetu" wordmark + tagline + Parent/Admin `RoleCard`s + footer) but reads **plain/generic** versus the premium bar the user is demanding. It also still shows the **fabricated** "Bank-grade encryption" pill (~line 250) and the "240+ schools · 38k parents trust us" strip (~lines 295–296) — the same fabricated copy tracked as **RA-S13**.
- **Evidence:** Screenshots `8kSUK8XT` / `bjPk5pfw`; `CommonLandingScreenV2.kt` fabricated pill + trust strip; `FeaturedInstitutionsSection` falls back to `landing_school_1/2/3.webp`.
- **Impact:** First impression undersells the product, and the fabricated metrics are an honesty/LAW-6 violation on the most-visible screen.
- **Fix spec:** **(Honesty)** remove or data-source the "Bank-grade encryption" pill and the schools/parents strip (drive from CMS/`LandingViewModel` real counts, or drop). **(Polish)** elevate the visual hierarchy with V* tokens — richer hero treatment, stronger type scale, real campus imagery in `FeaturedInstitutionsSection`, more deliberate spacing/elevation on the lifted sheet and `RoleCard`s — without changing the Parent+Admin entry contract. Not a rebuild; an honesty + craft pass. (RA-S13 is the honesty subset of this finding.)

---

# DEFINITION OF DONE

A fix is **DONE** only when ALL of the following hold (static-verifiable; the human builds locally):

1. **Security (TIER -1):** logout demonstrably clears the bearer cache (`clearToken()` invoked); no client-trusted `school_id` for authorization; new routes scope via `require*Context()`.
2. **Honesty (LAW 6):** zero fabricated names/numbers/grades rendered in any production portal. Every data surface is either real (behind `VStateHost`) or an unmistakable, label-free "coming soon" teaser.
3. **Auth UX:** returning parent login shows **only** OTP (no name field); new family signup still collects name. New-parent onboarding reaches the link-child screen.
4. **Notifications parity:** every cross-role write that affects another user (admin message, admin results-publish, link-request decision) emits a `Notify`, matching the teacher path's shape.
5. **Messaging:** parent and admin can **initiate** a conversation (compose-new + recipient picker), not only reply.
6. **Multi-child correctness:** a single shared selected-child drives all parent tabs incl. fees.
7. **Cosmetic truth:** avatar shows the real name; bell dots and nav badges reflect real counts.
8. **Design-system compliance:** all UI changes use only V* primitives + `VTheme` tokens (no Material defaults, no new ad-hoc tokens).
9. **No regressions:** existing reply paths, teacher writes, announcement/analytics flows still function (verified by reading the diffs, since builds are out of scope here).
10. **People management (RA-S17):** the People tab has Teachers / Students / Non-teaching-staff sub-tabs, each with search/filter; rows open real DB-backed profiles; **delete lives inside the profile** behind `VConfirmDialog`; the direct row "Remove" button is gone; non-teaching staff exists end-to-end (table + route + UI).
11. **Attendance feedback (RA-S18):** the teacher Submit button reflects the **real** network result (driven by `isSubmitting`/`submitSuccess`, not `stateful=true`), disables/confirms on success, and surfaces errors.
12. **Parent visibility (RA-S19):** attendance is presented as a **month calendar** with per-day status; marks resolve class/section from the linked student row (no silent `"A"` fallback) and show an explicit empty state; no fabricated grades remain (ties to RA-S11).
13. **Landing honesty + polish (RA-S20):** no fabricated metrics/security claims on the landing; visual hierarchy elevated within the V* system; Parent+Admin entries preserved.

---

# FIX EXECUTION ORDER

Fix top-down. Each tier should be a focused commit set.

### TIER -1 — Security (do first, blocks release)
- **RA-S01 🔴** Clear the Ktor bearer cache on logout (`clearToken()` + `SessionManager.clearAuthCache()` from `logout()`).
- Re-affirm RA-S14 invariants when touching any route (no body-sourced `school_id`).

### TIER 0 — Session, routing & honesty integrity
- **RA-S10 🔴** Remove fabricated students from `PewsPreview` (admin home).
- **RA-S11 🔴** Remove fabricated grades from `AiReportCardPreview` (parent academics).
- **RA-S04 🟡→treat as TIER0** Re-enable the parent child-link gate in `NavGraphV2` (broken onboarding path).

### TIER 1 — HIGH user-facing bugs
- **RA-S02 🟠** Gate the parent OTP-step name field on `AuthFlow.SIGNUP_PHONE` *(newly reported — the user's explicit ask)*.
- **RA-S18 🟠** Fix the teacher attendance Submit button to reflect real success (drop `stateful=true`; drive from `isSubmitting`/`submitSuccess`; surface errors) *(newly reported)*. **Quick win — UI-only.**
- **RA-S08 🟠** Notify recipient on admin direct message.
- **RA-S09 🟠** Notify parents on admin results-publish.
- **RA-S07 🟠** ✅ Compose-new (recipient picker) shipped for admin & parent messaging (parent via new `/recipients` endpoint).
- **RA-S19 🟠** Parent attendance → month-calendar view; harden the marks class/section join + explicit empty state *(newly reported)*.
- *(Verify & possibly add)* link-request decision notifications (DOMAIN 7 note).

### TIER 1b — HIGH feature build (larger scope)
- **RA-S17 🟠 ✅ DONE** Admin People sub-tabs (Teachers/Students/Non-teaching staff) + search/filter; delete moved into profiles behind `VConfirmDialog`; **direct row Remove button removed**; **non-teaching-staff vertical built end-to-end** (table + route + model/API/repo + VM + profile). Shipped across `93bfbde`/`eb4fece`/`b7ebd21`/`c8a918b`/`936021a`.

### TIER 2 — MEDIUM correctness & UX
- **RA-S05 🟡 ✅ DONE** Shared selected-child across parent ViewModels; thread `childId` into fees — `8350888` (server) + `9d85d01` (shared).
- **RA-S03 🟡 ✅ DONE** Persist & surface the user's display name (`USER_NAME` pref) — `4066ff4` (data) + `d2a8ac7` (surfacing).
- **RA-S06 🟡 ✅ DONE** Real avatar name + count-driven bell dots (parent + admin home) — `d2a8ac7`.
- **RA-S12 🟡 ✅ DONE** Drive the Comms nav badge from a real count (`71291b1`).

### TIER 3 — MEDIUM polish
- **RA-S13 🟡 / RA-S20 🟡** Replace fabricated landing social proof + "Bank-grade encryption" pill with data-sourced/honest copy, and elevate the landing's visual hierarchy within the V* system *(RA-S20 newly reported; RA-S13 is its honesty subset)*.

### TIER 4 — LOW / housekeeping
- Triage the 5 `TODO/FIXME` markers; convert any user-impacting ones into tracked findings.
- Keep RA-S14 / RA-S15 / RA-S16 as green references; do not re-investigate.

---

## ADDENDUM — User-requested deliverable: Legal & Support surface (Privacy / Terms / Help Desk)

**Requested 2026-06-09:** *"Privacy Policy, Terms of Service, Help Desk (minimal not much). support email: littlebridge.team@gmail.com"*

### LEGAL-01 · ✅ DONE · Public Privacy Policy / Terms of Service / Help Desk screen wired into the landing footer
- **Problem (pre-fix):** The landing footer's SUPPORT group ("Privacy Policy", "Terms of Service", "Help Desk") and the "Terms & Privacy Policy" continue-footnote were **static, non-interactive text** — there were no actual legal/support documents anywhere in the app, and no support contact was exposed.
- **Fix:**
  - **New screen** `composeApp/.../ui/v2/screens/auth/LegalInfoScreenV2.kt` — a single public surface (Light tone, V* primitives only) with a `VTopTabs` switcher for the three documents: **Privacy**, **Terms**, **Help Desk**. Back chevron returns to the landing screen.
    - **Privacy Policy** — minimal, honest, current-phase copy: what we collect (account / school-linkage / academic / messages), how we use it, what we never do (no selling, no third-party ad tracking, no cross-school sharing), the school-scoped access model, and retention/deletion via the Help Desk.
    - **Terms of Service** — using the app, accounts & access, content & communication, availability ("as is"), changes, contact.
    - **Help Desk** — minimal support surface. A tappable card opens the device mail composer (`LocalUriHandler.openUri("mailto:…")`) to **`littlebridge.team@gmail.com`** (single source of truth: `const val SUPPORT_EMAIL`). Includes "what to include" guidance + 3 common-question FAQs.
  - **Navigation** `NavGraphV2.kt` — added an `UnauthRoute.Legal` leaf + a `legalDoc: LegalDoc` state so a footer link opens the screen on the requested document; system-back returns to `Landing`.
  - **Landing wiring** `CommonLandingScreenV2.kt` — new `onLegal: (LegalDoc) -> Unit` param. The footer SUPPORT links and the continue-footnote ("Terms" / "Privacy Policy") are now **clickable** and route into the screen on the matching document. PORTALS group remains informational (auth CTAs stay on the entry cards).
  - **In-portal support (authenticated users)** — the "Help & support" row now opens the device mail composer to `SUPPORT_EMAIL` in all three role surfaces: `ParentProfileScreenV2`, `TeacherProfileScreenV2`, and `SchoolSettingsScreenV2` (admin). Each row sub-label now shows the address instead of the old "Contact VidyaSetu" placeholder.
- **Files:** `LegalInfoScreenV2.kt` [created], `NavGraphV2.kt` [modified], `CommonLandingScreenV2.kt` [modified], `ParentProfileScreenV2.kt` [modified], `TeacherProfileScreenV2.kt` [modified], `SchoolSettingsScreenV2.kt` [modified].
- **Support email:** `littlebridge.team@gmail.com` (centralised as `SUPPORT_EMAIL`).

---

*End of audit — 2026-06-09. Static analysis only; all line references are against branch `backend-by-abuzar_v1.0.2` @ `414a73c`.*
